# 解决文档点击后持续404轮询问题 - 用户指南

## 问题症状

当你点击文档列表中的文档时，会出现以下情况：
- 浏览器持续发送网络请求
- 开发者工具中看到大量404错误
- 页面可能无响应或加载缓慢
- 文档详情页显示"文档不存在"

## 问题原因

数据库中的文档已被删除，但浏览器缓存了旧的文档列表。当你点击这些不存在的文档时，前端的KG状态轮询机制会持续尝试获取状态，导致404错误。

## 解决方案

### 方法1：使用紧急缓存清理页面（推荐）

1. 在浏览器中访问：`http://localhost:5173/emergency-cache-clear.html`
2. 点击"清理缓存并刷新"按钮
3. 等待页面自动刷新
4. 重新登录（如果需要）

### 方法2：手动清理浏览器缓存

#### Chrome/Edge浏览器：
1. 按 `F12` 打开开发者工具
2. 点击顶部的 `Application`（应用程序）标签
3. 在左侧找到 `Storage`（存储）
4. 点击 `Clear site data`（清除网站数据）
5. 确认清除
6. 按 `Ctrl+Shift+R`（Windows）或 `Cmd+Shift+R`（Mac）强制刷新

#### Firefox浏览器：
1. 按 `F12` 打开开发者工具
2. 点击顶部的 `Storage`（存储）标签
3. 右键点击左侧的域名
4. 选择 `Delete All`（删除全部）
5. 按 `Ctrl+Shift+R`（Windows）或 `Cmd+Shift+R`（Mac）强制刷新

#### Safari浏览器：
1. 按 `Cmd+Option+E` 打开开发者工具
2. 点击 `Storage`（存储）标签
3. 选择 `Local Storage` 和 `Session Storage`
4. 删除所有项目
5. 按 `Cmd+Shift+R` 强制刷新

### 方法3：使用浏览器隐私模式测试

1. 打开浏览器的隐私/无痕模式
2. 访问应用：`http://localhost:5173`
3. 登录并测试功能
4. 如果正常工作，说明确实是缓存问题，请使用方法1或2清理缓存

## 预防措施

为了避免将来再次出现此问题：

1. **定期清理缓存**
   - 每周清理一次浏览器缓存
   - 使用紧急缓存清理页面

2. **注意数据库操作**
   - 删除文档前确认前端已关闭
   - 使用API删除文档而不是直接操作数据库

3. **使用开发者工具监控**
   - 保持开发者工具的Network标签打开
   - 注意异常的重复请求

## 技术细节（开发者参考）

### 已实施的修复

1. **useKGStatus Hook改进**
   - 检测404错误并自动停止轮询
   - 添加详细的日志记录
   - 改进错误处理逻辑

2. **DocumentDetail页面改进**
   - 检测404错误并重定向到文档列表
   - 添加友好的错误提示

3. **API Service改进**
   - 实现1秒缓存TTL
   - 避免重复请求
   - 改进错误消息

### 代码位置

- Hook: `client/src/hooks/useKGStatus.ts`
- API Service: `client/src/services/api.ts`
- Document Detail: `client/src/pages/DocumentDetail.tsx`
- Documents List: `client/src/pages/Documents.tsx`

## 常见问题

### Q: 清理缓存后需要重新登录吗？
A: 是的，清理localStorage会删除登录令牌，需要重新登录。

### Q: 清理缓存会丢失数据吗？
A: 不会。清理的只是浏览器本地缓存，服务器上的数据不受影响。

### Q: 为什么会出现这个问题？
A: 这是因为数据库被清空了，但浏览器缓存了旧的文档列表。这是开发过程中的常见情况。

### Q: 如何确认问题已解决？
A: 清理缓存并刷新后，文档列表应该是空的（如果数据库中没有文档），不应该再看到404错误。

## 需要帮助？

如果以上方法都无法解决问题，请：
1. 检查浏览器控制台的错误信息
2. 检查Network标签中的请求详情
3. 联系开发团队并提供错误截图
