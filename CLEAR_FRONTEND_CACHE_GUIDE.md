# 清除前端缓存指南

## 问题描述
前端在不断请求已删除文档的知识图谱状态，导致 404 错误。

## 解决方案

### 方法 1：清除浏览器缓存和本地存储（推荐）

1. 打开浏览器开发者工具（F12 或 Cmd+Option+I）
2. 进入 Application 标签页
3. 在左侧找到 "Local Storage" 和 "Session Storage"
4. 删除所有存储的数据
5. 刷新页面（Cmd+Shift+R 或 Ctrl+Shift+R）

### 方法 2：使用隐私模式
1. 打开浏览器的隐私/无痕模式
2. 访问应用 http://localhost:5173
3. 这样可以避免缓存问题

### 方法 3：清除特定的本地存储

在浏览器控制台（Console）中执行：

```javascript
// 清除所有本地存储
localStorage.clear();
sessionStorage.clear();

// 刷新页面
location.reload();
```

### 方法 4：重启前端服务

```bash
# 停止前端服务
# 然后重新启动
cd client
npm run dev
```

## 验证

清除缓存后，你应该看到：
- 没有 404 错误
- 文档列表为空
- 可以正常上传新文档

## 自动构建已启用

现在 `AUTO_BUILD_KG=true`，当你上传新文档时：
1. 文档会保存到数据库
2. 系统会自动触发知识图谱构建
3. 你可以在前端看到构建进度

## 注意事项

- 自动构建会消耗较多资源（CPU、内存、API 调用）
- 如果文档较大，构建可能需要几分钟
- 可以在后端日志中看到构建进度
