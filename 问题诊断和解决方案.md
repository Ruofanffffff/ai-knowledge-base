# 问题诊断和解决方案总结

## 问题 1: 文件上传重复检测和进度显示

### 状态: ✅ 已修复，等待测试

### 问题描述
- 上传重复文件时，前端没有显示重复检测模态框
- 上传过程中没有进度提示

### 根本原因
1. **字段名不匹配**: 后端返回 `duplicate: true`，前端检查 `isDuplicate` 字段
2. **缺少调试日志**: 无法追踪上传流程

### 已实施的修复
修改了 `client/src/pages/DocumentsList.tsx`:
- ✅ 支持两种字段名: `isDuplicate` 和 `duplicate`
- ✅ 添加详细的 console.log 调试日志:
  - 文件上传开始
  - 进度更新（百分比、速度、预计时间）
  - 响应数据
  - 重复检测确认

### 测试步骤
1. 打开浏览器开发者工具 (F12)
2. 切换到 Console 标签
3. 上传一个新文件，观察控制台日志
4. 再次上传相同文件，确认：
   - 控制台显示重复检测日志
   - 页面显示重复检测模态框
5. 如果问题仍存在，将控制台日志截图发给我

---

## 问题 2: 知识图谱可视化混乱

### 状态: 🔴 需要配置数据库

### 问题描述
- 知识图谱界面显示混乱，有很多无关节点
- 看不到上传文档生成的知识图谱

### 根本原因 ⚠️ 重要发现

您的系统使用**两个独立的数据库**：

#### SQLite 数据库 (`data/users.db`)
- **用途**: 用户数据、文档列表
- **状态**: ✅ 正常工作
- **文档**: 已保存 3 个文档

#### PostgreSQL 数据库 (Prisma)
- **用途**: 知识图谱数据 (CKB、实体、关系)
- **状态**: ❌ 未运行（开发阶段未安装）
- **结果**: 知识图谱数据无法保存

### 为什么知识图谱没有生成？

```
上传文档 → 保存到 SQLite ✅
         ↓
    触发知识图谱构建
         ↓
    尝试连接 PostgreSQL ❌
         ↓
    连接失败，数据未保存 ❌
         ↓
    可视化界面显示空白/混乱 ❌
```

### 解决方案

我推荐**方案 2**（最适合您的开发环境）：

#### 方案 2: 配置 Prisma 使用 SQLite ⭐ 推荐

**优点**:
- ✅ 无需安装 PostgreSQL
- ✅ 与现有数据库一致
- ✅ 开发调试简单
- ✅ 数据本地存储
- ✅ 后续可轻松迁移到 PostgreSQL

**步骤**:

1. **修改 `prisma/schema.prisma` 第 7-9 行**:
```prisma
datasource db {
  provider = "sqlite"
  url      = "file:../data/knowledge_graph.db"
}
```

2. **修改 `.env` 第 8 行**:
```bash
DATABASE_URL="file:./data/knowledge_graph.db"
```

3. **运行迁移命令**:
```bash
npx prisma generate
npx prisma migrate dev --name init_sqlite
```

4. **重启服务器**:
```bash
# 停止当前服务器 (Ctrl+C)
npm start
```

5. **重新上传测试文档**，验证知识图谱生成

---

## 其他可选方案

### 方案 1: 安装 PostgreSQL（生产环境推荐）

```bash
# macOS 安装
brew install postgresql@14
brew services start postgresql@14
createdb knowledge_base

# 运行迁移
npx prisma migrate deploy
npx prisma generate
```

### 方案 3: Docker PostgreSQL（快速测试）

```bash
docker run -d \
  --name postgres-kg \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=knowledge_base \
  -p 5432:5432 \
  postgres:14

npx prisma migrate deploy
npx prisma generate
```

---

## 下一步

### 对于问题 1（文件上传）
请按照测试步骤测试，如有问题请提供控制台日志截图。

### 对于问题 2（知识图谱）
**请告诉我您选择哪个方案**，我将帮您完成配置。

推荐选择**方案 2（SQLite）**，因为：
- 最适合开发阶段
- 配置最简单
- 与您现有的 SQLite 数据库一致

配置完成后，我们将：
1. ✅ 重新上传测试文档
2. ✅ 验证知识图谱生成
3. ✅ 检查可视化界面
4. ✅ 确认文档关联正确

---

## 技术细节

### 知识图谱模块依赖
所有 KG 模块都使用 Prisma Client:
- `kg/ckb/ckb_store.js` - CKB 存储
- `kg/entity/entity_store.js` - 实体存储
- `kg/relation/relation_store.js` - 关系存储
- `kg/hooks/document_hooks.js` - 文档钩子

### Prisma 配置位置
- Schema: `prisma/schema.prisma`
- 环境变量: `.env`
- 生成的 Client: `node_modules/@prisma/client`

### 数据库文件位置（方案 2）
- 用户数据: `data/users.db`
- 知识图谱: `data/knowledge_graph.db`（新建）
