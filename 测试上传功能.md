# 测试上传功能指南

## ✅ 前端已重新编译

前端代码已成功重新编译（刚刚完成）。新的编译文件包含了所有调试日志和修复。

## 🔄 下一步操作

### 1. 重启服务器

如果服务器正在运行，请重启：

```bash
# 按 Ctrl+C 停止当前服务器
# 然后运行：
npm start
```

### 2. 清除浏览器缓存

**非常重要**：必须清除浏览器缓存才能加载新的 JavaScript 文件

**方法 1：硬性重新加载**
1. 打开浏览器开发者工具（F12）
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

**方法 2：快捷键**
- Windows/Linux: Ctrl + Shift + R
- Mac: Cmd + Shift + R

**方法 3：手动清除**
1. 打开浏览器设置
2. 找到"清除浏览数据"
3. 选择"缓存的图片和文件"
4. 点击"清除数据"

### 3. 打开浏览器控制台

1. 按 F12 打开开发者工具
2. 切换到 Console 标签
3. 确保控制台是空的或清除旧日志

## 🧪 测试步骤

### 测试 1：上传新文件（验证进度显示）

1. 点击"上传文件"按钮
2. 选择一个文件（建议 > 1MB 以便看到进度）
3. **观察控制台**，应该看到：
   ```
   [Upload] 开始上传文件: {name: "xxx.docx", size: 123456, type: "..."}
   [Upload] 进度更新: {fileName: "xxx.docx", progress: 25, speed: 102400, estimatedTime: 3}
   [Upload] 进度更新: {fileName: "xxx.docx", progress: 50, speed: 98304, estimatedTime: 2}
   ...
   [Upload] 收到响应: {success: true, ...}
   ```
4. **观察页面右下角**，应该看到上传面板显示：
   - 文件名
   - 进度条（0-100%）
   - 上传速度（例如：2.5 MB/s）
   - 预计剩余时间（例如：剩余 3秒）

### 测试 2：上传重复文件（验证重复检测）

1. 再次上传刚才上传的同一个文件
2. **观察控制台**，应该看到：
   ```
   [Upload] 开始上传文件: {name: "xxx.docx", ...}
   [Upload] 进度更新: ...
   [Upload] 收到响应: {success: false, duplicate: true, ...}
   [Upload] 检测到重复文件: {fileName: "xxx.docx", duplicateType: "content", ...}
   [Upload] 重复检测模态框应该显示
   ```
3. **观察页面**，应该弹出重复检测模态框，显示：
   - 标题："检测到重复文件"
   - 重复类型（内容重复/文件名重复/完全重复）
   - 新文件信息（文件名、大小）
   - 现有文件信息（文件名、大小、上传时间）
   - 三个按钮：
     - 🔴 覆盖现有文件
     - 🟣 保存为新文件
     - ⚪ 取消上传

### 测试 3：重复文件处理

**测试 3.1：覆盖现有文件**
1. 上传重复文件
2. 在模态框中点击"覆盖现有文件"
3. 观察文件列表，应该只有一个文件（被覆盖）

**测试 3.2：保存为新文件**
1. 上传重复文件
2. 在模态框中点击"保存为新文件"
3. 观察文件列表，应该有两个文件（文件名会自动添加后缀）

**测试 3.3：取消上传**
1. 上传重复文件
2. 在模态框中点击"取消上传"
3. 观察上传面板，该文件应该从列表中移除

## ❌ 如果没有看到日志

### 检查清单

1. **确认已清除浏览器缓存**
   - 尝试在隐私/无痕模式下打开浏览器
   - 或者使用不同的浏览器测试

2. **检查 JavaScript 文件是否最新**
   - 打开开发者工具 → Network 标签
   - 勾选"Disable cache"
   - 刷新页面
   - 找到 `index-*.js` 文件
   - 检查文件大小是否约为 442 KB（刚编译的版本）

3. **检查控制台过滤器**
   - 确保控制台没有过滤掉 log 级别的消息
   - 检查是否有"Hide network"等过滤器开启

4. **检查浏览器设置**
   - 确保 JavaScript 已启用
   - 确保没有浏览器扩展阻止脚本执行

## 📊 预期结果

### 成功的标志

✅ 控制台显示 `[Upload]` 开头的日志
✅ 上传面板显示进度条和速度
✅ 重复文件时弹出模态框
✅ 可以选择覆盖/保存为新文件/取消

### 如果仍有问题

请提供以下信息：

1. **浏览器控制台截图**
   - 包含完整的日志输出（或说明没有日志）

2. **Network 标签截图**
   - 找到 `/api/documents/upload` 请求
   - 展开查看 Request Headers 和 Response

3. **操作步骤**
   - 详细描述您的操作
   - 上传的文件类型和大小
   - 是否是重复文件

4. **浏览器信息**
   - 浏览器类型（Chrome/Firefox/Safari/Edge）
   - 浏览器版本
   - 操作系统

## 🎯 关键点

1. **必须清除浏览器缓存** - 这是最常见的问题
2. **必须重启服务器** - 确保加载新的编译文件
3. **打开控制台** - 在上传前就打开，不要错过日志
4. **使用较大文件测试** - 小文件上传太快，可能看不到进度

## 📝 测试记录

建议记录测试结果：

- [ ] 清除了浏览器缓存
- [ ] 重启了服务器
- [ ] 打开了浏览器控制台
- [ ] 测试 1：上传新文件 - 看到日志？ ___  看到进度？ ___
- [ ] 测试 2：上传重复文件 - 看到日志？ ___ 看到模态框？ ___
- [ ] 测试 3：处理重复文件 - 覆盖成功？ ___ 保存为新文件成功？ ___

---

**准备好了吗？开始测试吧！** 🚀
