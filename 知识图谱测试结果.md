# 知识图谱生成流程测试结果

## 测试时间
2026-02-11

## 测试文档
- 文档名称: 美兰机场商汤科技联合创新协议暨第一阶段方案（初稿）
- 文档ID: 3
- 文件类型: .docx

## 测试配置（性能优先模式）
```javascript
{
  llmClient: null,              // ✅ 禁用 LLM
  enableSemanticRelations: false,  // ✅ 禁用语义关系
  enableQualityFilter: false      // ✅ 禁用质量过滤
}
```

## 测试结果

### ✅ 性能表现
- **总耗时**: 0.13秒 (133ms)
- **CKB数量**: 141个
- **平均每个CKB**: 0.95ms
- **性能评级**: ⭐⭐⭐⭐⭐ 优秀

### ❌ 核心问题：0 实体生成

```
CKBs: 141  ✅
实体: 0    ❌
关系: 0    ❌
```

## 问题诊断

### 1. 字段提取正常
- 成功提取了字段（从日志可以看到 Rule extraction 和 NER extraction 都在工作）
- 使用了缓存机制提升性能
- 字段提取策略: rule-first（Rule + NER）

### 2. Schema 匹配阶段可能存在问题
从日志可以看到：
- 处理了 15 个批次的 CKB
- 每个批次都显示 "entities: 0"
- 这表明 Schema 匹配或实体构建环节出现了问题

### 3. 可能的原因

#### 原因 A: Schema 匹配失败
- 提取的字段无法匹配到任何 Schema
- Schema 的完整度阈值过高（默认 40%）
- 映射表缺失或不完整

#### 原因 B: 实体构建条件不满足
- 锚点字段缺失
- 核心字段覆盖率不足
- 实体构建逻辑的前置条件未满足

#### 原因 C: LLM 禁用的影响
- 某些 Schema 可能依赖 LLM 进行字段映射
- 禁用 LLM 后，字段标准化失败
- 导致无法构建实体

## 性能优化效果

### 优化前（参考之前的测试）
- 平均每个CKB: 30,000-180,000ms（30-180秒）
- 性能瓶颈: LLM 调用、语义关系构建

### 优化后（当前测试）
- 平均每个CKB: 0.95ms
- **性能提升**: 31,579 - 189,474 倍 🚀
- **速度提升**: 600-2000倍（保守估计）

## 下一步计划

### 立即执行：诊断 0 实体问题

根据你的计划："需要修改 field_extractor.js,添加LLM client的提前检查,预计可以将性能提升3-6倍"

但现在的核心问题不是性能，而是**准确性**：
1. ✅ 性能已经优化到极致（0.95ms/CKB）
2. ❌ 但是生成了 0 个实体

### 建议的诊断步骤

1. **检查 Schema 匹配日志**
   - 查看哪些 Schema 被匹配
   - 查看字段映射的成功率
   - 查看完整度计算结果

2. **检查实体构建条件**
   - 查看锚点字段配置
   - 查看核心字段覆盖率要求
   - 查看实体构建的前置条件

3. **测试平衡配置**
   ```javascript
   {
     llmClient: yourLLMClient,  // 启用 LLM
     enableSemanticRelations: false,  // 仍然禁用
     enableQualityFilter: true  // 启用质量过滤
   }
   ```

## 结论

### 性能方面 ✅
- 优化措施非常成功
- 性能提升 600-2000 倍
- 平均每个CKB仅需 0.95ms

### 准确性方面 ❌
- 0 实体生成是严重问题
- 需要诊断 Schema 匹配和实体构建流程
- 可能需要启用 LLM 来提高字段映射成功率

### 权衡建议
- **快速预览场景**: 使用当前配置（但需要修复 0 实体问题）
- **生产环境**: 建议使用平衡配置（启用 LLM + 质量过滤）
- **实时处理**: 当前性能已经足够快，可以考虑启用更多功能

## 附录：测试日志摘要

```
[KG Service] Parsing document 3 (type: docx)...
Parsed 141 CKBs from document 3
[KG Service] Created 141 CKBs
[KG Service] Extracting fields...
[KG Service] Field extraction batch 1/8 completed
...
[KG Service] Field extraction batch 8/8 completed
[KG Service] Matching schemas and building entities...
[KG Service] Processed batch 1/15, entities: 0
...
[KG Service] Processed batch 15/15, entities: 0
[KG Service] Created 0 entities  ❌
[KG Service] Knowledge graph built in 133ms
```
