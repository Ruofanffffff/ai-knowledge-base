# 个人智能知识库系统架构设计

## 一、整体架构概述

系统采用**前后端分离**的桌面应用架构，基于Electron构建，实现本地优先运行。架构分为五大层次：表现层、应用层、服务层、数据层和AI层。各层职责清晰，通过定义良好的接口进行通信，确保系统的可扩展性和可维护性。

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                表现层（Electron + React）                      │
├───────────────────────┬───────────────────────┬─────────────────────────────────┤
│    内容展示组件       │    交互组件           │    可视化组件                   │
│ （文档、笔记、搜索结果）│（编辑、导入、导出）   │（知识图谱、统计图表）           │
└───────────────────────┴───────────────────────┴─────────────────────────────────┘
                             │               │               │
                             ▼               ▼               ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                应用层（前端业务逻辑）                          │
├───────────────────────┬───────────────────────┬─────────────────────────────────┤
│    内容管理模块       │    搜索模块           │    创作模块                     │
│ （导入、组织、备份）   │（语义搜索、关键词搜索）│（编辑器、模板、外部集成）       │
└───────────────────────┴───────────────────────┴─────────────────────────────────┘
                             │               │               │
                             ▼               ▼               ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                服务层（后端API）                               │
├───────────────────────┬───────────────────────┬─────────────────────────────────┤
│    文件处理服务       │    知识管理服务       │    AI服务                        │
│ （解析、OCR、格式转换）│（CRUD、标签、关联）   │（嵌入、实体识别、推荐）         │
└───────────────────────┴───────────────────────┴─────────────────────────────────┘
                             │               │               │
                             ▼               ▼               ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                数据层（数据库）                                │
├───────────────────────┬───────────────────────┬─────────────────────────────────┤
│    SQLite（结构化数据）│    ChromaDB（向量数据）│    文件系统（原始文件）          │
│ （元数据、标签、关系）  │（文本嵌入、语义搜索）   │（文档、图片、附件）             │
└───────────────────────┴───────────────────────┴─────────────────────────────────┘
                             │               │               │
                             ▼               ▼               ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                AI层（本地AI模型）                              │
├───────────────────────┬───────────────────────┬─────────────────────────────────┤
│    嵌入模型           │    NLP模型            │    可选：本地大语言模型         │
│ （SentenceTransformers）│（NLP.js、spaCy.js）   │（Ollama + Llama 2）            │
└───────────────────────┴───────────────────────┴─────────────────────────────────┘
```

## 二、各层详细设计

### 1. 表现层
表现层基于Electron+React+TypeScript构建，负责用户界面和交互体验。

**核心组件：**
- **文档阅读器**：展示各类文档内容，支持高亮、目录导航
- **编辑器**：基于Slate.js的Markdown编辑器，支持双向链接
- **搜索界面**：语义搜索和关键词搜索的输入输出界面
- **知识图谱可视化**：基于D3.js的实体关系图展示
- **控制面板**：系统设置、导入导出、备份恢复

### 2. 应用层
应用层实现前端业务逻辑，连接表现层和服务层。

**核心模块：**
- **内容管理模块**：处理内容的导入、导出、组织、版本控制
- **搜索模块**：处理搜索请求，展示搜索结果，管理搜索历史
- **创作模块**：管理编辑器状态，处理模板，与外部工具集成
- **推荐模块**：根据当前上下文展示相关内容
- **用户设置模块**：管理用户偏好和系统配置

### 3. 服务层
服务层基于Node.js+Express构建，提供RESTful API接口，处理核心业务逻辑。

**核心服务：**
- **文件处理服务**：
  - 多格式解析（PDF、Word、Markdown、TXT）
  - OCR处理（Tesseract.js）
  - 文件转换（格式转换、图片压缩）
- **知识管理服务**：
  - 内容CRUD操作
  - 标签管理和自动标签生成
  - 知识关联和关系管理
  - 版本控制和备份恢复
- **AI服务**：
  - 文本嵌入生成（SentenceTransformers）
  - 实体识别和提取（NLP.js、spaCy.js）
  - 语义搜索（ChromaDB集成）
  - 智能推荐算法

### 4. 数据层
数据层负责数据的持久化存储，采用多数据库策略。

**核心存储：**
- **SQLite**：存储结构化数据
  - 内容元数据（标题、作者、创建时间等）
  - 标签系统
  - 知识关系（实体关联）
  - 用户设置
- **ChromaDB**：存储向量数据
  - 文本嵌入（用于语义搜索）
  - 向量索引
- **文件系统**：存储原始文件
  - 上传的文档
  - 图片和附件
  - 备份文件

### 5. AI层
AI层集成本地AI模型，实现智能功能。

**核心模型：**
- **嵌入模型**：SentenceTransformers（all-MiniLM-L6-v2）用于生成文本嵌入
- **NLP模型**：
  - NLP.js：实体识别、关键词提取
  - spaCy.js：实体关系抽取
- **可选：本地大语言模型**：
  - Ollama + Llama 2：用于更高级的问答和生成功能
  - 可配置开关，支持按需加载

## 三、核心数据流向

### 1. 内容导入流程
```
用户上传文件 → 前端应用层 → 文件处理服务 → 解析内容/提取元数据 → 生成嵌入向量 → 存储到数据库（SQLite + ChromaDB）
```

### 2. 语义搜索流程
```
用户输入查询 → 前端应用层 → AI服务 → 生成查询向量 → ChromaDB搜索相似向量 → SQLite获取完整内容 → 前端展示结果
```

### 3. 知识图谱构建流程
```
内容导入/编辑 → NLP服务 → 实体识别 → 关系提取 → 存储实体和关系 → 前端可视化展示
```

### 4. 关联推荐流程
```
用户查看文档 → 应用层获取当前文档信息 → AI服务分析关联 → 数据库查询相关内容 → 前端展示推荐
```

## 四、架构优势

1. **本地优先**：所有核心功能在本地运行，无网络环境下可用
2. **前后端分离**：清晰的职责划分，便于开发和维护
3. **模块化设计**：各层通过接口通信，便于扩展和替换组件
4. **多数据库策略**：结构化数据和向量数据分离存储，优化性能
5. **可扩展性**：预留本地大语言模型接口，支持未来功能升级
6. **跨平台**：基于Electron，支持Windows、macOS、Linux

## 五、关键技术实现点

1. **Electron主进程与渲染进程通信**：使用IPC（Inter-Process Communication）实现前端与后端的通信
2. **本地向量数据库集成**：ChromaDB的本地实例化和向量索引构建
3. **多格式解析**：pdf-parse、Tesseract.js、docx等库的集成和优化
4. **知识图谱可视化**：D3.js实现节点和关系的动态展示
5. **语义搜索优化**：向量索引的构建和查询优化
6. **数据备份与恢复**：SQLite数据库备份和文件系统备份

## 六、未来扩展架构

当需要接入更复杂的AI功能时，可通过配置启用本地大语言模型：

```
AI层 ──► Ollama服务 ──► Llama 2模型
        └─► Mistral模型
        └─► 自定义模型
```

扩展架构保持向后兼容，用户可根据硬件性能选择是否启用高级AI功能。