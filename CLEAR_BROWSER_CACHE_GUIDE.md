# 清除浏览器缓存完整指南

## 问题确认

✅ **已确认**: 这是浏览器缓存问题

**数据库中的实际文档**:
1. 不妥协画质的直播该如何
2. 美兰机场商汤科技联合创新协议暨第一阶段方案（初稿）
3. 20210824海南省海口市美兰机场智慧防疫项目测试方案
4. test-file

**前端显示的缓存文档**:
1. 天花板维修流量通道说明
2. 基层机构编制和岗位设置的规范性说明一体化方案（初稿）
3. 20210624年度市级行政单位机构编制动态调整流程方案
4. test.file

这些文档完全不匹配，说明前端缓存了旧数据。

---

## 解决方案（按推荐顺序）

### 方案1: 清除网站数据（最彻底）⭐ 推荐

1. **打开开发者工具**
   - Windows/Linux: 按 `F12` 或 `Ctrl+Shift+I`
   - Mac: 按 `Cmd+Option+I`

2. **进入 Application 标签**
   - 在开发者工具顶部找到 "Application" 或"应用程序"标签
   - 如果找不到，点击 `>>` 按钮查看更多标签

3. **清除网站数据**
   - 在左侧面板找到 "Storage" 或"存储"
   - 点击 "Clear site data" 或"清除网站数据"按钮
   - 确认清除

4. **刷新页面**
   - 按 `F5` 或点击刷新按钮

### 方案2: 硬刷新（快速但可能不彻底）

1. **执行硬刷新**
   - Windows/Linux: 按 `Ctrl+Shift+R` 或 `Ctrl+F5`
   - Mac: 按 `Cmd+Shift+R`

2. **如果还不行，尝试**
   - 右键点击刷新按钮
   - 选择"清空缓存并硬性重新加载"

### 方案3: 手动清除特定缓存

#### 清除 LocalStorage
1. 打开开发者工具（F12）
2. 进入 Console/控制台 标签
3. 输入以下命令并回车：
   ```javascript
   localStorage.clear();
   sessionStorage.clear();
   location.reload();
   ```

#### 清除 Service Worker
1. 打开开发者工具（F12）
2. 进入 Application/应用程序 标签
3. 左侧选择 "Service Workers"
4. 点击 "Unregister" 注销所有 Service Worker
5. 刷新页面

#### 清除 Cache Storage
1. 打开开发者工具（F12）
2. 进入 Application/应用程序 标签
3. 左侧选择 "Cache Storage"
4. 右键每个缓存，选择 "Delete"
5. 刷新页面

### 方案4: 清除浏览器所有缓存

#### Chrome/Edge
1. 按 `Ctrl+Shift+Delete` (Windows) 或 `Cmd+Shift+Delete` (Mac)
2. 选择时间范围："全部时间"
3. 勾选：
   - ✅ 浏览历史记录
   - ✅ Cookie 及其他网站数据
   - ✅ 缓存的图片和文件
4. 点击"清除数据"
5. 重新打开网站

#### Firefox
1. 按 `Ctrl+Shift+Delete` (Windows) 或 `Cmd+Shift+Delete` (Mac)
2. 选择时间范围："全部"
3. 勾选：
   - ✅ 浏览和下载历史
   - ✅ Cookie
   - ✅ 缓存
4. 点击"立即清除"
5. 重新打开网站

#### Safari
1. 菜单栏 → Safari → 偏好设置
2. 进入"隐私"标签
3. 点击"管理网站数据"
4. 找到 localhost:5173
5. 点击"移除"
6. 重新打开网站

---

## 验证修复

清除缓存后，你应该看到：

✅ **正确的文档列表**:
1. 不妥协画质的直播该如何
2. 美兰机场商汤科技联合创新协议暨第一阶段方案（初稿）
3. 20210824海南省海口市美兰机场智慧防疫项目测试方案
4. test-file

❌ **不应该看到**:
1. 天花板维修流量通道说明
2. 基层机构编制和岗位设置的规范性说明一体化方案（初稿）
3. 20210624年度市级行政单位机构编制动态调整流程方案

---

## 如果还是不行

### 检查 Network 请求

1. 打开开发者工具（F12）
2. 进入 Network/网络 标签
3. 刷新页面
4. 找到 `/api/documents` 请求
5. 查看响应内容

**应该看到的响应**:
```json
[
  {
    "id": "4",
    "title": "不妥协画质的直播该如何",
    ...
  },
  {
    "id": "3",
    "title": "美兰机场商汤科技联合创新协议暨第一阶段方案（初稿）",
    ...
  },
  ...
]
```

**如果看到旧数据**:
- 检查响应头中的 `Cache-Control`
- 应该包含: `no-store, no-cache, must-revalidate`

### 检查是否有代理或CDN

如果你使用了代理服务器或CDN，它们可能也缓存了数据：

1. 检查是否使用了代理
2. 清除代理缓存
3. 或者暂时禁用代理

### 尝试隐私/无痕模式

1. 打开浏览器的隐私/无痕模式
   - Chrome/Edge: `Ctrl+Shift+N`
   - Firefox: `Ctrl+Shift+P`
   - Safari: `Cmd+Shift+N`
2. 访问 http://localhost:5173
3. 查看是否显示正确的文档

如果隐私模式下显示正确，说明确实是缓存问题。

---

## 预防未来缓存问题

### 开发者工具设置

1. 打开开发者工具（F12）
2. 进入 Network/网络 标签
3. 勾选 "Disable cache" / "禁用缓存"
4. 保持开发者工具打开状态

这样在开发时就不会有缓存问题。

### 浏览器设置

**Chrome/Edge**:
1. 设置 → 隐私和安全 → Cookie 及其他网站数据
2. 选择"退出所有窗口时清除 Cookie 和网站数据"

**Firefox**:
1. 设置 → 隐私与安全
2. 历史记录 → 使用自定义历史记录设置
3. 勾选"Firefox 关闭时清除历史记录"

---

## 技术说明

### 为什么会有缓存？

1. **浏览器默认行为**: 浏览器会缓存API响应以提高性能
2. **之前没有缓存控制**: 后端API之前没有设置 `Cache-Control` 头
3. **前端没有缓存失效**: 前端没有实现缓存失效机制

### 我们做了什么修复？

1. ✅ 添加了 `Cache-Control` 头到后端API
2. ✅ 修复了API路径错误（双重/api）
3. ⏳ 但旧的缓存数据仍在浏览器中

### 为什么需要手动清除？

- 浏览器不会自动清除已有的缓存
- 新的 `Cache-Control` 头只对新请求生效
- 旧的缓存数据需要手动清除

---

## 联系支持

如果尝试了所有方法仍然无法解决，请提供：

1. 浏览器类型和版本
2. 开发者工具 Network 标签的截图
3. `/api/documents` 请求的响应内容
4. 响应头信息

---

**更新时间**: 2026-02-12
**状态**: 等待用户清除浏览器缓存
