# 知识图谱生成缓慢原因分析

## 问题现状

文档ID=2有241个CKB块，处理非常缓慢。

## 性能瓶颈分析

### 1. **串行处理架构** ⚠️ 主要瓶颈

当前代码采用完全串行处理：

```javascript
// Step 2: 逐个CKB提取字段
for (const ckb of ckbs) {  // 241次循环
  const rawFields = await fieldExtractor.extractFields(ckb);  // 等待完成
  ckb.extracted_fields = rawFields;
}

// Step 3: 逐个CKB匹配Schema和构建实体
for (const ckb of ckbs) {  // 241次循环
  const schemaMatches = await schemaMatcher.matchSchemas(...);  // 等待完成
  for (const match of schemaMatches) {
    const normalizedFields = await fieldNormalizer.normalizeFields(...);  // 等待完成
    const entity = await entityBuilder.buildEntity(...);  // 等待完成
    await entityStore.saveEntity(entity);  // 等待完成
  }
}
```

**时间复杂度**: O(n) 其中 n=241
**预计时间**: 如果每个CKB处理需要2-5秒，总时间 = 241 × 3秒 = 12分钟

### 2. **LLM调用频繁** 🔥 次要瓶颈

每个CKB都可能触发LLM调用：

```javascript
// field_extractor.js
if (useLLM && llmExtractor.shouldUseLLM(allFields, minFieldCount)) {
  console.log('Rule+NER extraction insufficient, using LLM fallback');
  const llmFields = await llmExtractor.extractFieldsWithLLM(ckb, allFields);
}
```

**LLM调用场景**:
- 字段提取不足时（< 3个字段）
- 字段归一化时（可选）
- 实体名称生成时（可选）
- 语义关系提取时（可选）

**预计LLM调用次数**: 
- 字段提取: 241次（如果Rule+NER不足）
- 字段归一化: 每个匹配的Schema × CKB数量
- 实体构建: 每个实体
- 总计可能超过500次LLM调用

**每次LLM调用耗时**: 1-3秒
**总LLM时间**: 500 × 2秒 = 16分钟

### 3. **数据库写入频繁** 💾

每个操作都立即写入数据库：

```javascript
// 保存CKB: 241次
for (const ckb of ckbs) {
  await ckbStore.saveCKB(ckb);
}

// 保存实体: 每个实体1次
await entityStore.saveEntity(entity);

// 保存关系: 每个关系1次
await relationStore.createRelation(relation);
```

**数据库操作次数**: 241 (CKB) + N (实体) + M (关系) ≈ 500-1000次
**每次操作耗时**: 10-50ms
**总数据库时间**: 1000 × 30ms = 30秒

### 4. **Schema匹配开销** 📊

每个CKB都要与150+个Schema进行匹配：

```javascript
const schemas = await schemaManager.listSchemas({ active: true });  // 150+ schemas
for (const ckb of ckbs) {
  const schemaMatches = await schemaMatcher.matchSchemas(ckb.extracted_fields, schemas);
}
```

**匹配次数**: 241 CKBs × 150 Schemas = 36,150次匹配
**每次匹配耗时**: 1-5ms
**总匹配时间**: 36,150 × 3ms = 108秒 ≈ 2分钟

### 5. **置信度更新开销** 🔄

处理完所有实体后，逐个更新置信度：

```javascript
for (const entity of docEntities) {
  await confidenceEngine.updateEntityConfidence(entity.entity_id);
}
```

**更新次数**: 每个实体1次
**每次更新耗时**: 50-200ms（需要查询关系图）
**总更新时间**: 如果有100个实体 = 100 × 100ms = 10秒

## 总时间估算

| 步骤 | 耗时 | 占比 |
|------|------|------|
| CKB解析 | 10秒 | 3% |
| 字段提取（含LLM） | 12分钟 | 40% |
| Schema匹配 | 2分钟 | 7% |
| 字段归一化（含LLM） | 8分钟 | 27% |
| 实体构建 | 3分钟 | 10% |
| 关系构建 | 2分钟 | 7% |
| 置信度更新 | 10秒 | 1% |
| 质量过滤 | 1分钟 | 3% |
| 数据库写入 | 30秒 | 2% |
| **总计** | **≈30分钟** | **100%** |

## 优化建议

### 🚀 立即可实施（高优先级）

#### 1. 批量并行处理
```javascript
// 将串行改为并行
const fieldExtractionPromises = ckbs.map(ckb => 
  fieldExtractor.extractFields(ckb).catch(err => {
    console.error(`Field extraction failed for ${ckb.ckb_id}:`, err);
    return [];
  })
);
const allExtractedFields = await Promise.all(fieldExtractionPromises);
```

**预期提升**: 从30分钟降至5-8分钟（提升75%）

#### 2. 批量数据库写入
```javascript
// 收集所有CKB后批量写入
await ckbStore.saveCKBsBatch(ckbs);

// 收集所有实体后批量写入
await entityStore.saveEntitiesBatch(entities);
```

**预期提升**: 数据库时间从30秒降至3秒（提升90%）

#### 3. 减少LLM调用
```javascript
// 提高LLM fallback阈值
const options = {
  minFieldCount: 5,  // 从3提高到5
  useLLM: false      // 对于简单文档禁用LLM
};
```

**预期提升**: LLM调用减少50-70%，时间从16分钟降至5分钟

#### 4. 启用缓存
```javascript
// field_extractor已支持缓存
const options = {
  useCache: true  // 确保启用
};
```

**预期提升**: 重复内容处理时间降至0

### 🔧 中期优化（中优先级）

#### 5. Schema匹配优化
- 使用索引加速匹配
- 预过滤不相关的Schema
- 使用向量相似度快速筛选

**预期提升**: 匹配时间从2分钟降至20秒

#### 6. 分批处理
```javascript
// 每次处理50个CKB
const batchSize = 50;
for (let i = 0; i < ckbs.length; i += batchSize) {
  const batch = ckbs.slice(i, i + batchSize);
  await processBatch(batch);
}
```

**预期提升**: 内存占用降低，避免超时

### 🎯 长期优化（低优先级）

#### 7. 使用消息队列
- 将KG生成任务放入队列
- 后台worker处理
- 支持进度查询

#### 8. 增量更新
- 只处理变更的CKB
- 复用已有实体和关系

#### 9. 分布式处理
- 多机器并行处理
- 负载均衡

## 推荐方案

### 方案A: 快速优化（1小时实施）
1. ✅ 禁用LLM fallback（useLLM: false）
2. ✅ 批量数据库写入
3. ✅ 并行处理CKB（Promise.all）

**预期效果**: 30分钟 → 3-5分钟

### 方案B: 完整优化（1天实施）
1. ✅ 方案A的所有优化
2. ✅ Schema匹配优化
3. ✅ 分批处理
4. ✅ 进度显示

**预期效果**: 30分钟 → 1-2分钟

## 立即行动

我建议先实施**方案A**，快速解决当前问题：

1. 修改 `kg/services/kg_service.js` 添加并行处理
2. 修改 `kg/ckb/ckb_store.js` 添加批量保存
3. 修改 `kg/entity/entity_store.js` 添加批量保存
4. 修改 `kg/relation/relation_store.js` 添加批量保存

是否需要我立即实施这些优化？
