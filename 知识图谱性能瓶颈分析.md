# 知识图谱性能瓶颈深度分析

## 执行时间: 2025-02-11

## 问题现象
- 测试脚本运行超过3分钟(180秒)后超时
- 即使采用了并行处理,性能仍然很慢

## 根本原因分析

### 🔴 主要瓶颈 #1: 数据库操作未真正并行化

#### 位置: `kg/services/kg_service.js`

**问题代码片段:**

```javascript
// Step 3: 虽然使用了 Promise.all 并行处理 CKB
const batchPromises = batch.map(async (ckb) => {
  // ...
  const normalizedFields = await fieldNormalizer.normalizeFields(...);
  const entity = await entityBuilder.buildEntity(...);
  // ...
});

// 但是在批次完成后,使用了 串行保存
if (allEntities.length > 0) {
  try {
    await entityStore.saveEntities(allEntities);  // ✅ 这里是批量保存
  } catch (error) {
    // Fallback 到串行保存
    for (const entity of allEntities) {
      await entityStore.saveEntity(entity);  // ❌ 串行保存
    }
  }
}
```

**分析:**
1. 实体构建阶段已经并行化 ✅
2. 但是关系构建和保存仍然是串行的 ❌

```javascript
// Step 4: 构建内置关系 - 串行循环
for (const ckb of ckbs) {
  if (!ckb.entities || ckb.entities.length === 0) continue;
  
  const builtinRelations = await builtinRelationBuilder.buildBuiltinRelations(ckb.entities);
  allBuiltinRelations.push(...builtinRelations);
}

// 然后批量保存关系
if (allBuiltinRelations.length > 0) {
  await relationStore.createRelations(allBuiltinRelations);
}
```

### 🔴 主要瓶颈 #2: 字段标准化(Field Normalization)可能很慢

#### 位置: `kg/services/kg_service.js` Line 108-120

```javascript
await Promise.all(
  batch.map(async (ckb) => {
    // ...
    // 标准化字段 - 可能调用 LLM
    const normalizedFields = await fieldNormalizer.normalizeFields(
      ckb.extracted_fields,
      match.schema,
      { llmClient }  // ⚠️ 如果启用 LLM,这里会很慢
    );
    // ...
  })
);
```

**问题:**
- 如果 `fieldNormalizer.normalizeFields` 调用 LLM,每次调用可能需要 1-3 秒
- 即使并行处理,如果有大量 CKB,总时间仍然很长

### 🔴 主要瓶颈 #3: 实体名称生成可能调用 LLM

#### 位置: `kg/entity/entity_builder.js` Line 70-150

```javascript
async function generateCanonicalName(fields, schema, ckb, options = {}) {
  // ...
  
  // Step 4: LLM作为兜底方案 - 100%启动验证和优化
  if (useLLM) {
    const llmResult = await enhanceNameWithLLM(
      canonicalName,
      schema,
      ckb,
      llmClient,
      !isWellFormed,
      tempEntity
    );
    // ...
  }
}
```

**问题:**
- 每个实体都可能调用 LLM 来优化名称
- 如果有 100 个实体,就是 100 次 LLM 调用
- 即使并行,LLM API 也有速率限制

### 🔴 主要瓶颈 #4: 置信度更新是串行的

#### 位置: `kg/services/kg_service.js` Line 280-295

```javascript
// Step 7: 更新置信度分数 (并行批次处理)
const confidenceBatchSize = 20;
for (let i = 0; i < docEntities.length; i += confidenceBatchSize) {
  const batch = docEntities.slice(i, i + confidenceBatchSize);
  
  await Promise.all(
    batch.map(async (entity) => {
      await confidenceEngine.updateEntityConfidence(entity.entity_id);
    })
  );
}
```

**问题:**
- 虽然使用了批次并行,但每个批次之间是串行的
- 如果有 200 个实体,分 10 个批次,每个批次等待最慢的那个

### 🔴 主要瓶颈 #5: 数据库查询效率低

#### 位置: `kg/entity/entity_store.js` Line 250-280

```javascript
async function getAllEntities(options = {}) {
  const entities = await prisma.kGEntity.findMany({
    skip,
    take,
    orderBy: { [orderBy]: order }
  });
  
  // ⚠️ 对每个实体都要反序列化 JSON 字段
  const deserialized = entities.map(deserializeEntity);
  return deserialized;
}

function deserializeEntity(dbEntity) {
  return {
    entity_id: dbEntity.id,
    entity_type: dbEntity.type,
    canonical_name: dbEntity.canonicalName,
    aliases: dbEntity.aliases ? JSON.parse(dbEntity.aliases) : [],  // ⚠️ JSON 解析
    schemas: dbEntity.schemas ? JSON.parse(dbEntity.schemas) : {},
    supported_by: dbEntity.supportedBy ? JSON.parse(dbEntity.supportedBy) : [],
    attributes: dbEntity.attributes ? JSON.parse(dbEntity.attributes) : {},
    // ...
  };
}
```

**问题:**
- 每次查询都要解析多个 JSON 字段
- 如果查询 1000 个实体,就是 1000 次 JSON 解析

## 性能优化建议

### 优先级 1: 减少 LLM 调用

**方案 A: 禁用 LLM 增强**
```javascript
const result = await kgService.buildKnowledgeGraph(
  doc.id,
  filePath,
  doc.fileType,
  {
    llmClient: null,  // ✅ 禁用 LLM
    enableSemanticRelations: false,
    enableQualityFilter: true
  }
);
```

**方案 B: 降低 LLM 使用概率**
```javascript
// 在 entity_builder.js 中
const nameResult = await generateCanonicalName(
  fieldsObj,
  schemaScore.schema,
  ckb,
  { 
    useLLM: true, 
    llmProbability: 0.1  // ✅ 从 0.5 降低到 0.1
  }
);
```

### 优先级 2: 优化数据库批量操作

**当前问题:**
```javascript
// 关系构建是串行的
for (const ckb of ckbs) {
  const builtinRelations = await builtinRelationBuilder.buildBuiltinRelations(ckb.entities);
  allBuiltinRelations.push(...builtinRelations);
}
```

**优化方案:**
```javascript
// 并行构建关系
const relationPromises = ckbs
  .filter(ckb => ckb.entities && ckb.entities.length > 0)
  .map(ckb => builtinRelationBuilder.buildBuiltinRelations(ckb.entities));

const relationResults = await Promise.all(relationPromises);
const allBuiltinRelations = relationResults.flat();
```

### 优先级 3: 优化置信度更新

**当前问题:**
- 批次之间串行等待

**优化方案:**
```javascript
// 完全并行更新置信度
await Promise.all(
  docEntities.map(entity => 
    confidenceEngine.updateEntityConfidence(entity.entity_id)
      .catch(error => {
        console.error(`Confidence update failed for ${entity.entity_id}:`, error);
        return null;
      })
  )
);
```

### 优先级 4: 添加数据库索引

**检查是否有索引:**
```sql
-- 检查 KGEntity 表的索引
PRAGMA index_list('KGEntity');

-- 检查 KGRelation 表的索引
PRAGMA index_list('KGRelation');
```

**建议添加的索引:**
```prisma
model KGEntity {
  id            String   @id
  type          String   @index  // ✅ 添加索引
  canonicalName String   @index  // ✅ 添加索引
  confidence    Float    @index  // ✅ 添加索引
  // ...
}

model KGRelation {
  sourceId   String  @index  // ✅ 添加索引
  targetId   String  @index  // ✅ 添加索引
  type       String  @index  // ✅ 添加索引
  // ...
}
```

### 优先级 5: 使用连接池和事务优化

**当前问题:**
- 每次数据库操作都是独立的连接

**优化方案:**
```javascript
// 使用事务批量插入
await prisma.$transaction(async (tx) => {
  // 批量插入实体
  await tx.kGEntity.createMany({
    data: entities.map(serializeEntity),
    skipDuplicates: true
  });
  
  // 批量插入关系
  await tx.kGRelation.createMany({
    data: relations.map(serializeRelation),
    skipDuplicates: true
  });
});
```

## 立即可执行的优化

### 1. 创建优化版本的测试脚本

```javascript
// test-kg-performance-optimized.js
const result = await kgService.buildKnowledgeGraph(
  doc.id,
  filePath,
  doc.fileType,
  {
    llmClient: null,  // ✅ 禁用 LLM
    enableSemanticRelations: false,  // ✅ 禁用语义关系
    enableQualityFilter: false  // ✅ 禁用质量过滤
  }
);
```

### 2. 修改 kg_service.js 中的关系构建

将串行循环改为并行:

```javascript
// 并行构建内置关系
const builtinRelationPromises = ckbs
  .filter(ckb => ckb.entities && ckb.entities.length > 0)
  .map(ckb => builtinRelationBuilder.buildBuiltinRelations(ckb.entities));

const builtinRelationResults = await Promise.all(builtinRelationPromises);
const allBuiltinRelations = builtinRelationResults.flat();
```

### 3. 跳过置信度更新步骤

在测试时暂时禁用:

```javascript
// Step 7: 更新置信度分数 (可选,测试时禁用)
if (options.enableConfidenceUpdate !== false) {
  // ... 置信度更新代码
}
```

## 预期性能提升

| 优化项 | 当前耗时 | 优化后耗时 | 提升比例 |
|--------|---------|-----------|---------|
| 禁用 LLM | 100-150ms/entity | 10-20ms/entity | 80-90% |
| 并行关系构建 | 串行累加 | 并行最大值 | 50-70% |
| 跳过置信度更新 | 20-30ms/entity | 0ms | 100% |
| 数据库索引 | 全表扫描 | 索引查询 | 70-90% |

**总体预期:**
- 当前: 180+ 秒 (超时)
- 优化后: 10-30 秒 (预计)
- 提升: 6-18 倍

## 下一步行动

1. ✅ 创建优化版本的 kg_service.js
2. ✅ 创建优化版本的测试脚本
3. ✅ 运行测试验证性能提升
4. ✅ 如果仍然慢,使用性能分析工具定位具体瓶颈
