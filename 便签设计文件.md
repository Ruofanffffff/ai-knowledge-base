# 便签设计文件

便签功能是作为本产品面向用户的第一个界面，主要目的是收集用户的想法、素材，进一步转化为系统以及AI的核心物料，经过加工为用户提供灵感

便签创建
1. 创建入口
- app打开会弹出1-3秒的开机画面
- 开机画面后会直接进入“我的笔记”页面
[图片]
- 点击右上角“+”号（左图），可以进入创建页面（右图）。
[图片]

[图片]

  - 基础输入功能：支持用户输入以下内容。
    - 文本。输入完毕后，保存为结构化数据，存入数据库
    - 上传照片。上传完毕后，作为直接附件，保存进入对象存储。需要引入LLM，进行图像分析（文字识别及照片分析）。目的是将非结构化数据转化为结构化。如果用户提供的照片上都是文字，则提取文字，按照结构化进行保存；如果用户上传的照片是风景照、人物肖像、画等，则需要理解这个照片（包括但不限于：这个照片拍的是风景画？人物肖像？电影截图？还是什么？这个照片里有什么？照片中各个元素之间的关系是什么？照片使用了什么色彩？色彩搭配是什么？等等）。prompt如下：
      - System Prompt: 
你是一个专业的多模态信息分析引擎。
你的职责是将用户上传的图片内容，转换为结构化、可存储、可检索的数据。
你不与用户对话，只输出结构化结果。
禁止输出任何与任务无关的自然语言说明。
      - User Prompt:
任务目标：
请分析用户上传的图片内容，将其从非结构化视觉信息，转化为结构化数据。

你需要完成以下步骤：
1. 判断图片的主要类型（仅选一个最主要类型）
2. 根据图片类型，进行对应的深入分析
3. 如果图片中包含可识别文字，需进行文字识别（OCR）
4. 所有输出必须严格符合 JSON 结构

图片可能的类型包括但不限于：
- 纯文字图片（文档、截图、手写）
- 风景照片
- 人物肖像
- 物品 / 产品
- 艺术作品 / 绘画
- 电影 / 动画截图
- 多元素混合图片（需判断主类型）

分析规则：
- 不确定的信息必须标注置信度（confidence: 0~1）
- 不要猜测拍摄者身份或主观意图
- 不做价值判断（如“好看”“漂亮”）
- 所有字段必须可用于数据库存储与索引

    - 输出结构
  🌟 顶层统一结构（无论什么图）
{
  "image_type": "",
  "confidence": 0.0,
  "has_text": false,
  "ocr_text": "",
  "structured_content": {}
}

  ① 纯文字图片（文档 / 笔记 / 截图）
{
  "image_type": "text_document",
  "confidence": 0.95,
  "has_text": true,
  "ocr_text": "完整识别出的原始文字",
  "structured_content": {
    "language": "zh",
    "content_blocks": [
      {
        "block_type": "title",
        "text": ""
      },
      {
        "block_type": "paragraph",
        "text": ""
      },
      {
        "block_type": "list",
        "items": []
      }
    ]
  }
}

  ② 风景照片
{
  "image_type": "portrait",
  "confidence": 0.9,
  "has_text": false,
  "ocr_text": "",
  "structured_content": {
    "person_count": 1,
    "visible_attributes": {
      "face_visible": true,
      "pose": "frontal",
      "expression": "neutral"
    },
    "context": {
      "indoor_outdoor": "indoor",
      "background": "plain"
    },
    "color_analysis": {
      "dominant_colors": ["black", "white"],
      "color_tone": "neutral"
    }
  }
}

③ 人物肖像（⚠️ 注意避免敏感推断）
{
  "image_type": "portrait",
  "confidence": 0.9,
  "has_text": false,
  "ocr_text": "",
  "structured_content": {
    "person_count": 1,
    "visible_attributes": {
      "face_visible": true,
      "pose": "frontal",
      "expression": "neutral"
    },
    "context": {
      "indoor_outdoor": "indoor",
      "background": "plain"
    },
    "color_analysis": {
      "dominant_colors": ["black", "white"],
      "color_tone": "neutral"
    }
  }
}

④ 艺术作品 / 绘画 / 插画
{
  "image_type": "artwork",
  "confidence": 0.85,
  "has_text": false,
  "ocr_text": "",
  "structured_content": {
    "art_form": "painting",
    "style": "impressionist",
    "subject_elements": ["human figure", "nature"],
    "color_analysis": {
      "dominant_colors": ["warm yellow", "blue"],
      "color_palette": "warm"
    },
    "composition": {
      "focus": "center",
      "symmetry": "asymmetric"
    }
  }
}

⑤ 多元素混合图片（比如：照片 + 字）
{
  "image_type": "mixed",
  "confidence": 0.8,
  "has_text": true,
  "ocr_text": "...",
  "structured_content": {
    "primary_visual_type": "product",
    "secondary_elements": ["text_overlay"],
    "visual_elements": [],
    "text_summary": "图片中文字的简要结构化摘要"
  }
}

- 上传文档：依照系统现有的上传存储解析流程进行解析，保存进入数据库
- 上传表格：依照系统现有的上传存储解析流程进行解析，保存进入数据库

  - 进阶输入功能：依托系统的AI能力，可以将用户输入的信息通过AI进行编写，有如下功能：
智能生成
用户在app中选中文本，长按，会唤起如下按钮，选择第一个“智能生成”，系统会将用户输入的信息作为prompt提交给LLM进行扩写。参考prompt：
你是一个专业、克制、有审美判断力的内容与影像生成助手。

你的任务包含两部分：
1️⃣ 在忠于原意的前提下，对文本进行高质量扩写
2️⃣ 基于扩写后的内容，生成一条可直接用于“照片生成模型”的图像描述 Prompt

核心原则：
- 不改变原文立场、情绪与风格
- 不引入无关主题
- 避免空话、套路化表达
- 内容具体、有画面感、有信息密度
- 影像描述要真实、可拍、避免抽象词

文本扩写规则：
- 不重复原文句子
- 自然衔接原文
- 让内容“更完整”，而不是“更啰嗦”

照片生成规则：
- 以「真实摄影」为目标，而不是插画或概念图
- 描述清晰的：主体、环境、光线、构图、情绪
- 使用自然语言描述，不要堆砌无意义形容词
- 不出现文字、logo、水印
- 不出现人物特写时默认不指定具体人脸

输出格式必须严格如下：

【扩写后的文本】
（完整扩写内容）

【照片生成 Prompt】
（一段可直接提交给图像生成模型的英文或中文摄影描述）
[图片]

智能校对
用户在app中选中文本，长按，会唤起如下按钮，选择第二个“智能校对”，系统会将用户输入的信息作为prompt提交给LLM进行校对。参考prompt：
你是一名专业、严谨的中文文本校对助手。

你的任务是：在不改变原意、不改变写作风格的前提下，对用户提供的文本进行「智能校对」。

校对要求：
1. 仅修正以下问题：
  - 错别字、病句
  - 语法不通顺
  - 标点符号使用错误
  - 明显的用词不当
2. 不得：
  - 改写句式结构（除非原句明显不通）
  - 扩写内容或添加新信息
  - 删除用户原有的观点或情绪
  - 改变文本整体语气与风格
3. 如果原文本身没有明显问题，请原样输出，不要说明理由。
  
输出要求：
- 直接输出校对后的完整文本
- 不要添加任何解释、备注或说明
生成表格
用户在app中选中文本，长按，会唤起如下按钮，选择第三个“生成表格”，系统会将用户输入的信息作为prompt提交给LLM进行处理，以生成一个表格。参考prompt：
- 系统prompt 
你是一个“信息结构化助手”，擅长从自然语言中提取信息，并将其整理为清晰、准确、可读的表格。

规则：
1. 只基于用户提供的内容进行整理，不允许编造信息
2. 表格结构应符合内容本身的逻辑，而不是固定模板
3. 如果信息不完整，需要在表格中明确标注“原文未提及”
4. 优先保证表格对人类阅读友好，而非追求形式复杂
- 用户prompt
请将以下内容整理为一个表格。

要求：
- 自动判断最合适的表格类型（如信息表、对比表、清单、时间线）
- 表格需覆盖文本中的核心信息
- 表格结构清晰，字段命名简洁
- 不要添加原文中不存在的信息
  
文本内容：
"""
{{用户选中的原始文本}}
"""
- 输出结构
请以 JSON 格式输出，结构如下：

{
  "table_type": "...",
  "columns": ["列1", "列2", "..."],
  "rows": [
    ["行1列1", "行1列2", "..."],
    ["行2列1", "行2列2", "..."]
  ],
  "summary": "一句话说明该表格整理了什么"
}
生成脑图
用户在app中选中文本，长按，会唤起如下按钮，选择第四个“生成脑图”，系统会将用户输入的信息作为prompt提交给LLM进行处理，以生成一个表格。方法如下：
🔹 Step 1：文本理解 & 中心主题识别
目标：找“一个中心”
{
  "central_topic": "日本秘汤的旅行价值"
}
规则：
- 不是标题照抄
- 是「概念抽象」
🔹 Step 2：一级分支生成（并行拆解）
{
  "branches": [
    "秘汤的定义",
    "与豪华旅馆的对比",
    "旅行体验的变化",
    "个人偏好与态度"
  ]
}
原则：
- 互斥
- 覆盖全文
- 3–6 个为最佳
🔹 Step 3：二 / 三级节点拆分（递归）
{
  "branch": "与豪华旅馆的对比",
  "children": [
    "建筑形式",
    "服务与设施",
    "对旅行本质的影响"
  ]
}
🔹 Step 4：节点语义压缩（非常重要）
❌ 不要长句
 ✅ 用 短语 / 关键词
🔹 Step 5：结构化输出（树结构）
{
  "type": "mindmap",
  "central_topic": "...",
  "nodes": [
    {
      "id": "1",
      "text": "秘汤的定义",
      "children": [...]
    }
  ]
}

通用的json：
{
  "central_topic": "日本秘汤的旅行价值",
  "nodes": [
    {
      "id": "A",
      "text": "什么是秘汤",
      "children": [
        { "id": "A1", "text": "位置偏远" },
        { "id": "A2", "text": "自然环境" }
      ]
    },
    {
      "id": "B",
      "text": "为何更吸引我",
      "children": [
        { "id": "B1", "text": "反对奢华堆砌" },
        { "id": "B2", "text": "找回旅行本质" }
      ]
    }
  ]
}

prompt如下：
你是一个“脑图结构生成助手”，擅长将自然语言内容拆解为清晰的层级结构。

规则：
1. 先提炼一个能概括全文的中心主题
2. 将内容拆解为 3–6 个一级分支
3. 每个节点用简短关键词或短语表达
4. 层级清晰，避免重复与交叉
5. 不添加原文未提及的新观点

请将以下文本整理为一张脑图。

要求：
- 自动提炼中心主题
- 拆解清晰的层级结构
- 使用关键词而非长句
- 结构适合脑图展示
  
文本内容：
"""
{{用户选中的文本}}
"""
用户点击保存之后，均采用json方式将文本传递至后端；图片、视频的非结构化数据落对象存储保存

