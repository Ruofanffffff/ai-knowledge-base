# 知识图谱空白问题诊断报告

## 问题现状

用户上传了3个文档，但知识图谱页面显示为空白。

## 根本原因

经过诊断，发现以下问题：

### 1. 数据库状态
- ✅ Prisma数据库文件存在：`data/knowledge_graph.db`
- ✅ 所有必需的表已创建（kg_entities, kg_relations, ckb等）
- ❌ **但所有表都是空的（0条记录）**

### 2. 文档上传记录
```
文档ID | 标题 | 创建时间
1 | test-file | 2026-02-11 06:42:29
2 | 20210824海南省海口市美兰机场智慧防疫项目测试方案 | 2026-02-11 06:44:27
3 | 美兰机场商汤科技联合创新协议暨第一阶段方案（初稿） | 2026-02-11 06:44:59
```

### 3. 知识图谱钩子未触发

查看服务器日志发现：
- ✅ 文档上传成功
- ✅ 重复检测工作正常
- ❌ **没有任何 `[KG Hook]` 日志**
- ❌ **知识图谱生成从未被触发**

## 为什么会这样？

这3个文档是在**服务器重启之前**上传的。当时的服务器可能：
1. 知识图谱钩子未正确配置
2. 或者环境变量 `KG_ENABLED=false`
3. 或者钩子代码有bug

因此，这些文档上传时**没有触发知识图谱生成**。

## 解决方案

### 方案1：重新上传文档（推荐）

最简单的方法是删除现有文档，然后重新上传：

1. 在文档列表页面删除这3个文档
2. 重新上传文件
3. 这次上传会自动触发知识图谱生成

### 方案2：手动触发知识图谱生成

我已经创建了一个脚本 `trigger-kg-for-existing-docs.js`，可以为现有文档生成知识图谱。

**注意**：这个脚本需要较长时间运行（特别是对于大文档），因为它需要：
- 解析文档内容
- 提取实体和关系
- 调用LLM进行语义分析
- 存储到数据库

运行方法：
```bash
node trigger-kg-for-existing-docs.js
```

**预计时间**：
- test-file: ~2秒
- 美兰机场测试方案: ~5-10分钟（241个CKB块）
- 美兰机场协议: ~5-10分钟

### 方案3：配置环境变量

确保 `.env` 文件中没有禁用知识图谱：

```bash
# 确保这一行不存在，或者设置为 true
# KG_ENABLED=false  # ❌ 删除这行或改为 true
KG_ENABLED=true     # ✅ 启用知识图谱
```

## 前端问题：重复文件模态框未显示

### 问题描述
用户反馈：重新上传重复文件时，前端没有弹出模态框提示。

### 已实施的修复
1. ✅ 修复了后端响应字段名不匹配问题（`duplicate` vs `isDuplicate`）
2. ✅ 前端代码已更新，支持两种字段格式
3. ✅ 添加了详细的console.log调试日志

### 测试步骤
1. 访问 http://localhost:5173（注意是5173端口，不是3000）
2. 上传一个已存在的文件
3. 打开浏览器控制台（F12）
4. 查看 `[Upload]` 开头的日志
5. 应该会看到模态框弹出

### 如果模态框仍未显示

请检查浏览器控制台的日志，并提供以下信息：
1. 是否看到 `[Upload] 检测到重复文件` 日志？
2. 是否看到 `[Upload] 显示重复检测模态框` 日志？
3. 是否有任何错误信息？

## 下一步行动

### 立即行动
1. **选择方案1或方案2**来生成知识图谱数据
2. **刷新知识图谱页面**查看结果

### 验证步骤
1. 运行以下命令检查数据是否已生成：
```bash
sqlite3 data/knowledge_graph.db "SELECT COUNT(*) FROM kg_entities; SELECT COUNT(*) FROM kg_relations;"
```

2. 如果看到非零数字，说明数据已生成
3. 刷新浏览器的知识图谱页面（http://localhost:5173/graph）

## 技术细节

### 知识图谱生成流程
```
文档上传 
  ↓
onDocumentCreated 钩子触发
  ↓
解析文档 → 创建CKB块
  ↓
提取字段 → 匹配Schema
  ↓
构建实体 → 构建关系
  ↓
存储到 Prisma 数据库
  ↓
前端通过 API 获取并显示
```

### API端点
- 获取实体：`GET /api/knowledge-graph/entities`
- 获取关系：`GET /api/knowledge-graph/relations`
- 前端会自动调用这些API

## 联系支持

如果问题仍未解决，请提供：
1. 浏览器控制台的完整日志
2. 服务器日志（特别是包含 `[KG Hook]` 的部分）
3. 数据库查询结果
