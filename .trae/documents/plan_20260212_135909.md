To address the user's request, I will:

1.  **Set Default Model**: Change the default model for Hi Brain from `deepseek-chat` to `qwen-plus` in both the frontend (`client/src/pages/AISearch.tsx`) and the backend (`server.js`).
2.  **Verify Knowledge Base Integration**:
    *   The user mentioned that the assistant failed to use the "ai内涝" document from the knowledge base.
    *   I need to inspect the `loadDocuments()` function and the `/api/ai/search` endpoint in `server.js` to ensure documents are correctly loaded and indexed.
    *   I will verify if the search logic (keywords, vector search if available, or simple string matching) is robust enough to find "ai内涝". The current implementation uses simple keyword matching which might be failing if the query doesn't match exactly.
    *   I'll add a check to ensure `loadDocuments` is actually returning the expected data.

**Plan:**

1.  **Update `client/src/pages/AISearch.tsx`**:
    *   Change the initial state of `selectedModel` to `'qwen-plus'`.
    *   Reorder the `models` array to put `qwen-plus` at the top or mark it as default.

2.  **Update `server.js`**:
    *   In the `/api/ai/search` endpoint, check the default model logic (though the frontend usually sends the model).
    *   **Improve Search Logic**: The current search logic (lines 1470-1570 in `server.js`) splits the query into keywords and checks for inclusion in title/content.
        *   If the user asks "Did you use the document about AI waterlogging?", and the document title is "AI内涝", the current logic *should* find it if "AI内涝" is in the query.
        *   However, if the query is complex, the simple keyword extraction might fail.
        *   I will add a debug log to print the loaded documents to the console to verify if "ai内涝" is even in the `documents.json`.
        *   I'll also verify if the `loadDocuments()` function is correctly reading the latest data.

**Crucial Check**: The user said "the assistant said no". This implies the *search* might have failed or the *LLM* decided not to use it.
*   If the search returned the document, it would be in the prompt context.
*   If the search failed, the prompt context would be empty.
*   I will assume the search logic needs a slight improvement or the document isn't where we think it is.

**Action:**
1.  Set default model to `qwen-plus`.
2.  Add more robust logging to `/api/ai/search` to trace why a document might be missed.
3.  Ensure `loadDocuments` is working correctly.

**Refinement on Search Logic:**
The current logic splits query by spaces or Chinese punctuation. If the user types "AI内涝", it might be split into "AI", "内涝". If the document has "AI内涝" (no space), `titleLower.includes("ai")` is true, `titleLower.includes("内涝")` is true. This should work.
However, if the document content is not in `data/documents.json` but in the SQLite database (which `server.js` seems to be transitioning to, seeing `userDb` usage elsewhere but `loadDocuments` reading from JSON), that's a huge potential bug.

**Critical Finding in `server.js`**:
*   Lines 108-118: `loadDocuments` reads from `DATA_FILE` (`data/documents.json`).
*   Lines 624-689: `/api/documents` (GET) reads from `userDb` (SQLite).
*   **The Search API (`/api/ai/search`, line 1442) uses `loadDocuments()` (JSON file)!**
*   **The Upload API (`/api/documents`, line 745) writes to `userDb` (SQLite)!**

**MAJOR BUG**: The search endpoint is looking at a static JSON file (`mockDocuments` or `data/documents.json`), while new documents uploaded by the user are being saved to the SQLite database (`documents` table). The search function is completely ignoring the database!

**Fix Plan**:
1.  Modify `/api/ai/search` to fetch documents from `userDb` (SQLite) instead of `loadDocuments()` (JSON file).
2.  Set default model to `qwen-plus` in frontend.

This explains exactly why the assistant couldn't find the "AI内涝" document - it was likely uploaded to the database but the search was looking at an old JSON file.