To address the user's feedback ("The document content text is too tight against the display box... it looks weird, make it look like Word"), I need to adjust the padding and layout of the document content area in `client/src/pages/DocumentDetail.tsx`.

**Current Implementation:**

```tsx
<div className="max-w-4xl mx-auto bg-white shadow-md p-12 min-h-[800px] rounded-lg">
  <div className="prose max-w-none">
    <p className="text-slate-700 whitespace-pre-wrap leading-relaxed font-serif text-lg">{document.content}</p>
  </div>
</div>
```

I already added `p-12` (48px) padding, which should be decent. However, "tight against the display box" suggests the user might want even more padding, or perhaps the "box" (the viewport) is too tight around the "paper".

**Visualizing "Word Style":**

1. **Paper Aspect**: A Word document usually has substantial margins (e.g., 2.54cm \~ 96px). `p-12` is 48px, which is half of a standard 1-inch margin.
2. **Centered Layout**: The paper should float in the middle of a gray background.
3. **Spacing**: There should be space *around* the paper too, not just inside it.

**Proposed Changes:**

1. **Increase Paper Padding**: Change `p-12` (48px) to `px-20 py-16` (80px horizontal, 64px vertical) to mimic standard document margins more closely.
2. **Ensure Paper "Float"**: The outer container `flex-1 overflow-y-auto p-8` provides 32px gap around the paper. I'll increase this to `p-12` or ensure the paper has a max-width that allows it to breathe.
3. **Container Sizing**: The `max-w-4xl` (56rem = 896px) is a bit wide for A4 (usually \~210mm \~ 794px at 96dpi, or wider on screen). 896px is fine, but maybe `max-w-[850px]` is more A4-like.
4. **Justify Text**: Word documents often use justified text. I'll add `text-justify` to the paragraph.

**Plan:**

* **Target File**: `client/src/pages/DocumentDetail.tsx`

* **Locate**: The document rendering section (lines 222-227).

* **Modify**:

  * Increase padding: `p-12` -> `px-24 py-20` (96px horizontal \~ 1 inch).

  * Add text justification: `text-justify`.

  * Ensure the background contrast (`bg-slate-50/50`) helps the white paper pop.

```tsx
// New styles
<div className="max-w-[850px] mx-auto bg-white shadow-lg px-24 py-20 min-h-[1100px] rounded-sm">
  <div className="prose prose-lg max-w-none">
    <p className="text-slate-800 whitespace-pre-wrap leading-loose font-serif text-justify">
      {document.content}
    </p>
  </div>
</div>
```

* `max-w-[850px]`: Closer to A4 width.

* `px-24`: \~1 inch margin on sides.

* `min-h-[1100px]`: Closer to A4 height aspect ratio.

* `shadow-lg`: More depth.

* `rounded-sm`: Paper usually has sharp or slightly rounded corners, not large `rounded-lg`.

* `leading-loose`: More line spacing (Word default is often 1.15 or 1.5, `leading-relaxed` is 1.625, `loose` is 2. This might be too much, `relaxed` is probably fine).

**Refined Plan**:

1. Change container padding to `px-24 py-16` (approx 1-inch margins).
2. Change max-width to `max-w-4xl` (keep it responsive but spacious).
3. Add `text-justify` to the paragraph.
4. Change \`shadow

