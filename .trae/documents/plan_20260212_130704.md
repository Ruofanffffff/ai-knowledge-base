I will optimize the Hi Brain module by adding a persona definition and memory management capabilities.

1.  **Create Persona**: I will create `config/soul.md` containing the "Hi Brain" persona, adapted from the "OpenClaw soul.md" structure. This will define Hi Brain as a spiritual partner that learns from user documents.
2.  **Update Backend (`server.js`)**:
    *   Modify the `/api/ai/search` endpoint to accept conversation `history`.
    *   Update the model calling functions (`callCloudModel`, `callQwenModel`, `callDeepSeekModel`) to support a `systemPrompt` and conversation `history`.
    *   Inject the content of `soul.md` as the system prompt.
    *   Include conversation history in the API calls to the LLM to enable "Memory".
3.  **Update Frontend (`client/src/pages/AISearch.tsx`)**:
    *   Update the `handleSend` function to send the chat `messages` (history) to the backend API.

This will give Hi Brain a consistent personality and the ability to remember the conversation context.