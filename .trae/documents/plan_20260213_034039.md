I will optimize the mobile UI to address the layout issues reported.

### **1. Fix "Hi Brain" Header Layout (`AISearch.tsx`)**
- **Problem**: The title "Hi Brain" wraps to two lines, and the model selector is cut off on small screens.
- **Solution**:
  - Prevent the title from wrapping by adding `whitespace-nowrap`.
  - Adjust the flex layout of the header to better distribute space.
  - Set a `max-width` on the model selector or allow it to shrink (`shrink`) to fit within the available space without pushing other elements off-screen.
  - Use `text-ellipsis` for the model selector text to handle long model names gracefully.

### **2. Add Mobile Category Toggle to Documents (`Documents.tsx`)**
- **Problem**: The sidebar categories are hidden on mobile (as per previous fix), but users now have no way to filter documents.
- **Solution**:
  - Implement a **horizontal scrollable category bar** (or a dropdown/modal) at the top of the document list, visible *only on mobile* (`md:hidden`).
  - This allows users to select categories without needing the desktop sidebar, preserving the document card view.
  - The toggle will sync with the existing `selectedCategory` state.

### **3. Fix Document Detail Header Layout (`DocumentDetail.tsx`)**
- **Problem**: Long titles stretch the layout, and action buttons are cut off.
- **Solution**:
  - Apply `truncate` (or `line-clamp-2` for slightly more visibility) to the document title to prevent it from pushing the layout width.
  - Wrap the header content in a container that handles overflow correctly.
  - Adjust the button group to be more compact on mobile, possibly grouping secondary actions (Edit/Delete) into a "More" menu (`...` icon) or just ensuring they shrink/wrap correctly.

### **4. Build and Sync**
- Run `npm run build` and `npx cap sync` to apply changes to the Android app.