To address the user's request to keep the "AI 总结" button style consistent regardless of its toggle state (clicked vs. unclicked), I will modify the CSS classes in `client/src/pages/DocumentDetail.tsx`.

**Current Implementation:**
The button changes its style based on `showChatPanel` state:
*   Active (`showChatPanel === true`): `bg-purple-100 text-purple-700` (Light purple background, dark text)
*   Inactive (`showChatPanel === false`): `bg-gradient-to-r from-purple-500 to-indigo-600 text-white` (Gradient purple background, white text)

**The User's Request:**
"The icon... turned purple only after clicking. Request to change it so the icon style before clicking is the same as after clicking."
Wait, the user said "before clicking it was pure white".
Ah, looking at my previous code:
When `showChatPanel` is false (default), it has `bg-gradient... text-white`. The icon is white.
When `showChatPanel` is true (clicked), it has `bg-purple-100 text-purple-700`. The icon becomes purple.

The user wants the style to be **consistent**. They likely want the "Active" style (purple icon) to be the default, or the "Gradient" style to be the active one too.
Re-reading carefully: "Request to change it so the icon style before clicking is the same as after clicking."
This implies removing the state-based styling and keeping one consistent look.

**Design Choice:**
Since this is a primary action button, the **Gradient Purple (White Text)** style is more prominent and looks better as a "Call to Action".
However, if it's a toggle button, usually "pressed" state looks different.
But if the user insists on "same style", I will use the **Gradient Purple** style for both states, possibly adding a subtle shadow or ring for the active state instead of a complete color change.

**Refined Plan:**
1.  Locate the button in `client/src/pages/DocumentDetail.tsx` (lines 198-209).
2.  Remove the conditional class logic based on `showChatPanel`.
3.  Apply the `bg-gradient-to-r from-purple-500 to-indigo-600 text-white` style permanently.
4.  Add a `ring` or `shadow-inner` effect when `showChatPanel` is true to indicate it's active, but keep the color scheme identical as requested.

```tsx
className={`flex items-center gap-2 px-3 py-1.5 rounded-full text-sm font-medium transition-all bg-gradient-to-r from-purple-500 to-indigo-600 text-white shadow-sm hover:shadow-md ${
  showChatPanel ? 'ring-2 ring-purple-300 ring-offset-1' : ''
}`}
```
This keeps the "purple/white" look always, satisfying "same style before and after".

**Wait**, the user said "the icon was pure white before clicking... turned purple after".
My previous code:
False -> Gradient BG, White Icon.
True -> Light BG, Purple Icon.

If they want "same style", I will just stick to the Gradient one (White Icon) for both states.

**Action:** Update the button `className` to remove the conditional background/text color switching.