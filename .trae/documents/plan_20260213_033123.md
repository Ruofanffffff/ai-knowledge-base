I will fix the layout issues in both the AI Assistant (Dashboard) and the Documents page to ensure they are fully responsive on mobile devices.

### **1. Fix "Hi Brain" AI Assistant Layout (Dashboard)**
- **Problem**: On mobile, the chat interface might be getting cut off or not displaying the full history/input area correctly due to fixed heights or layout ordering.
- **Solution**:
  - In `client/src/pages/Dashboard.tsx`, I will verify and adjust the flex/grid behavior of the AI Assistant container.
  - Specifically, I will ensure the `AISearch` component takes up appropriate vertical space on mobile (currently `h-[600px]`, which is good, but I'll double-check its internal responsiveness).
  - *Note*: The user mentioned "only see new conversation, right side chat window not visible". This suggests a layout where maybe a sidebar or list is obscuring the chat on mobile, or the "new conversation" button is all that's visible. Since `Dashboard.tsx` already has `AISearch` prominent, I will ensure `AISearch` itself handles mobile views correctly (it might be splitting into a "history list" vs "chat area" internally, similar to the sidebar issue).

### **2. Fix "Documents" Page Layout**
- **Problem**: The sidebar with categories (`w-64`) is fixed-width and always visible, crushing the main content area on small screens.
- **Solution**:
  - In `client/src/pages/Documents.tsx`, I will hide the category sidebar on mobile devices by adding the `hidden md:block` class to its container (Line 190).
  - This allows the main document grid to occupy the full screen width on mobile, making cards visible and readable.
  - *Future Enhancement (Optional)*: A toggle button could be added to show categories on mobile, but simply hiding it first solves the critical "unusable" state.

### **3. Rebuild and Sync**
- After modifying the React components, I will run `npm run build` and `npx cap sync` to update the Android app with these layout fixes.

### **Refined Plan**
1.  **Modify `client/src/pages/Documents.tsx`**: Add `hidden md:block` to the sidebar `div`.
2.  **Modify `client/src/pages/Dashboard.tsx`**: Review the `AISearch` container. If `AISearch` has an internal sidebar (history list), I need to check `client/src/pages/AISearch.tsx` as well to ensure *its* sidebar is hidden on mobile so the chat window is visible.
    - *Self-Correction*: I should check `client/src/pages/AISearch.tsx` now to see if it has a master-detail layout that needs similar "hide sidebar on mobile" treatment.

(I will assume `AISearch.tsx` likely has a similar issue and will include checking/fixing it in the execution phase).