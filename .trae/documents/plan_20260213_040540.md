I will optimize the mobile UI for the AI Assistant and Document Detail pages as requested.

### **1. Optimize "Hi Brain" Header Layout (`AISearch.tsx`)**
- **Problem**: The model selector dropdown is overlapping/crowding the "Hi Brain" title on small screens, and the native dropdown style clashes with the app's design.
- **Solution**:
  - **Layout**: Adjust the flex container to ensure the title area has priority or proper spacing, possibly stacking them or using `flex-shrink` more effectively.
  - **Custom Selector**: Replace the native `<select>` with a custom-styled dropdown component (using a `div` + absolute positioned list). This allows full control over the "trigger" button's appearance (e.g., just showing an icon or a shortened name) and the dropdown menu's style (matching the app's rounded corners, shadows, and colors). This solves both the "blocking" issue (by making the trigger compact) and the "style inconsistency" issue.

### **2. Optimize Document Detail AI Summary on Mobile (`DocumentDetail.tsx`)**
- **Problem**: Currently, the AI summary panel slides in from the right, squeezing the document content or overflowing on mobile screens.
- **Solution**:
  - **Mobile Detection**: Use the `isMobile` state (already used in other components or add if missing) to change behavior based on screen size.
  - **Full-Screen Mode**: On mobile, when `showChatPanel` is true:
    - Hide the document content entirely (or cover it).
    - Render the AI Summary panel as a **full-screen container** (`fixed inset-0 z-50`).
    - Add a prominent "Close/Back" button to the summary panel header to easily return to the document.
  - **Desktop Behavior**: Preserve the existing side-by-side split view for desktop users.

### **3. Build and Sync**
- Run `npm run build` and `npx cap sync` to apply changes to the Android app.