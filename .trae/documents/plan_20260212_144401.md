To address the user's feedback ("Your logic is reversed, fix it"), I will re-examine the previous change and the user's earlier instruction.

**Previous User Instruction:** "The brain icon was pure white before clicking... turned purple after. Request to change it so the icon style before clicking is the same as after clicking."

**My Interpretation:** I thought they wanted the "Gradient/White" style everywhere.
**User's Feedback:** "Logic is reversed".

**New Interpretation:**
The user likely wants the **Purple Icon** (the "after clicking" style) to be the *consistent* style, or maybe I misunderstood which state was which.

Let's look at the original code (before my last edit):
*   Inactive: `text-purple-500` (Purple Icon on White/Transparent BG)
*   Active: `bg-purple-100 text-purple-700` (Purple Icon on Light Purple BG)

Wait, the user said "before clicking it was pure white". This refers to the **Gradient Button** I introduced in the step *before* that (`bg-gradient... text-white`).
So:
1.  I changed the icon to a gradient button (White text).
2.  The user said "It was white before, purple after. Make it consistent."
3.  I made it "Gradient/White" everywhere.
4.  User says "Logic reversed".

This implies they wanted the **Purple style** everywhere, NOT the White style.
OR, they meant "The icon style *after* clicking (Purple) should be the style *before* clicking too." -> So they want the Purple style.
OR, they meant "The icon style *before* clicking (White) should be the style *after* clicking too." -> This is what I did (Gradient/White everywhere).

If "Logic is reversed", and I did "White everywhere", then they must want "Purple everywhere".

**However**, let's look at the *original* original state (before I touched it today).
It was `text-purple-500` (Purple icon).
My *first* change today made it a Gradient Button (White icon).
Then the user complained about the color shift.

If I revert to a style that looks like the "Active" state (Light Purple BG, Purple Text) or just a simple Purple Button for both states, that might be it.

**But let's assume the "Reversed" comment refers to my last action.**
My last action: "Use Gradient (White) everywhere".
If that's reversed, they want "Use Light Purple (Purple) everywhere" OR "Use the *original* style".

Let's try to make it look like a standard **Ghost/Outline** button that is consistent, or a **Solid** button that is consistent.
Given the "AI Summary" label, a solid or light-bg button is best.

**Hypothesis:** The user liked the "Purple" look (which they saw "after clicking") and wants that to be the default.
So I will change the button to use the `bg-purple-100 text-purple-700` style (or similar) in **both** states, perhaps darkening it slightly on hover/active.

**Actually**, looking at the `bg-gradient` style... that's very "AI-like". Maybe they want *that* to be the active state too? I did that.
Let's try the other way:
**Plan:** Change the button to be `bg-white text-purple-600 border border-purple-200` (or similar "purple" style) by default, and keep it consistent.
**Wait**, "pure white" might refer to the *icon color* (white icon on gradient) vs *purple icon* (on light bg).
If I made it "White Icon everywhere", and they said "Reversed", they want "Purple Icon everywhere".

**Decision:** I will change the button style to be a **Light Purple background with Purple text/icon** (the "active" style from before) for **both** states. This matches the "Purple" look they seemed to prefer or implies the "Active" state is the desired constant look.

**Alternative Interpretation:** Maybe they meant the *toggle* logic? No, they said "icon style".

**Refined Plan:**
1.  Modify `DocumentDetail.tsx`.
2.  Change the button class to use `bg-purple-50 text-purple-600 hover:bg-purple-100` (consistent Purple style).
3.  Add `ring` for the active state to distinguish it, but keep colors consistent.

**Actually**, let's look at the *exact* user words: "brain icon... pure white before... purple after... change so before is same as after".
If "Before" = White, "After" = Purple.
"Change so Before is same as After" => Before should be Purple.
So they want **Purple** everywhere.

My previous change made it **White** everywhere. That is indeed "reversed".

**Action:** Update button to be Purple style (Light BG, Purple Text) everywhere.