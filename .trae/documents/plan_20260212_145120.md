To address the user's concern shown in the screenshot ("Don't squeeze the text to the far left, keep some margin"), I need to further refine the text rendering in `client/src/pages/DocumentDetail.tsx`.

**Analysis of the User's Screenshot/Issue:**
The user's screenshot (implied, as they referenced one) likely shows that despite my previous changes, the text is still hugging the left side of the container too closely, or perhaps the "container" itself is too wide/full-width on their screen.
Actually, looking at my code:

```tsx
<div className="max-w-[850px] mx-auto bg-white shadow-lg px-24 py-24 ...">
  <div className="prose prose-lg max-w-none">
    <p className="... indent-[2em]">{document.content}</p>
```

`px-24` is 96px. That is a *lot* of padding.
However, `prose` (Tailwind Typography) has its own max-width logic.
But `prose max-w-none` should disable the prose width constraint and let it fill the container (minus padding).

**Potential Issue:**
If the content is just a list of short lines (as seen in some "screenshot" descriptions where it's just a list), and they are left-aligned, it might *look* like it's squeezed to the left if the page is wide.
But the user specifically said "keep some margin" (maintain page margins).
Maybe the `whitespace-pre-wrap` is preserving original newlines that have no indentation? I added `indent-[2em]`, but that only affects the *first* line of a paragraph. If the document has many short lines (hard breaks), only the first one gets indented? No, every `<p>` gets indented. But here we have one giant `<p>` with `whitespace-pre-wrap`.
**Crucial Detail**: `whitespace-pre-wrap` respects newlines in the source string. It does *not* treat each newline as a new paragraph tag. It treats the whole thing as *one* paragraph.
**So** **`indent-[2em]`** **only affects the very first line of the entire document!**
Subsequent lines (after `\n`) are just lines in the same paragraph, so they are **flush left**. This explains why it looks "squeezed to the left" for everything after line 1.

**Solution:**

1. **Split the content by newlines** and render each paragraph separately. This will ensure *every* paragraph gets the `indent-[2em]` (first line indent) and proper spacing.
2. Alternatively, if we want to keep it simple, we can just use `whitespace-pre-wrap` but maybe the user *wants* padding on the left for *all* lines?

   * No, standard typography is: Indent first line, others flush left (but with a page margin).

   * If the user says "don't squeeze to the left", and my `px-24` (96px) is working, the text starts 96px from the paper edge. That is a huge margin.

   * Maybe the "paper" is hitting the screen edge?

   * `w-full` on the container vs `max-w-[850px] mx-auto`. On a small screen, 850px might fill it. But `px-24` inside should still be visible.

**Hypothesis**: The "indentation" issue is the most likely culprit for "text squeezed to left" if the user expects paragraph indentation for *every* block of text.
If I split by `\n` and render multiple `<p>` tags:

* Each `<p>` will have `indent-[2em]`.

* This mimics Word where "Enter" creates a new paragraph with indentation.

**Plan:**

1. Refactor the content rendering logic in `DocumentDetail.tsx`.
2. Instead of one `<p>{content}</p>`, use `{content.split('\n').map(...)}`.
3. For each paragraph:

   * Check if it's empty (render a `<br/>` or spacer).

   * If not empty, render `<p className="indent-[2em] mb-2 ...">`.

   * Remove `whitespace-pre-wrap` from the paragraph class since we are handling lines manually?

   * Actually, better to keep `whitespace-pre-wrap` on the inner text just in case, but usually splitting by `\n` handles the main breaks.

   * **Better**: Use `content.split(/\n+/)` to split by paragraphs.

**Revised Render Logic:**

```tsx
<div className="prose prose-lg max-w-none text-slate-800 leading-relaxed font-serif text-justify">
  {document.content.split('\n').map((paragraph, index) => (
    <p key={index} className="indent-[2em] min-h-[1.5em] my-1">
      {paragraph || <br/>}
    </p>
  ))}
</div>
```

This ensures every "line" (paragraph) in the source text gets the indentation.

**Wait**, if the source text has *soft wraps* vs *hard wraps*:

* If it's just long text wrapping, it's one paragraph.

* If it has `\n`, it's a new paragraph.

* Splitting by `\n` is correct for text files.

**Action:** Update `DocumentDetail.tsx` to render individual paragraphs.
