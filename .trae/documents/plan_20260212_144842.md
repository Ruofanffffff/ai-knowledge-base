To address the user's feedback ("The text should not be tight against the top/sides... keep margins consistent and beautiful"), I will refine the padding of the document content in `client/src/pages/DocumentDetail.tsx`.

**Current Implementation:**
```tsx
<div className="max-w-[850px] mx-auto bg-white shadow-lg px-24 py-20 min-h-[1100px] rounded-sm">
  <div className="prose prose-lg max-w-none">
    <p className="text-slate-800 whitespace-pre-wrap leading-relaxed font-serif text-justify">{document.content}</p>
  </div>
</div>
```
I used `px-24 py-20` (96px horizontal, 80px vertical).

**The Issue:**
If the user says "text should not be tight against the top" (don't top-align too much?), maybe they mean visual balance.
Or, perhaps the `whitespace-pre-wrap` is causing issues where lines are breaking weirdly or "tight against the box" means something else.
However, `px-24` is quite large (1 inch).
Wait, maybe they mean the *container* itself is tight against the viewport top?
"Document detail text don't be top-aligned" (literal translation of "不要顶格") usually means "don't start at the very top edge".
If they mean the *text inside the paper*, `py-20` (80px) is plenty.
If they mean the *paper inside the view*, `p-8` (32px) is the outer padding.

Let's assume they mean the **Page Margins** (inside the white paper).
"两边的页边距保持一致" -> "Keep side margins consistent".
Currently `px-24` sets both left and right padding to 24 units. This *is* consistent.
Maybe the `prose` class has its own max-width or margins that are interfering?
`prose` sets a `max-width: 65ch`. Inside a `max-w-[850px]` container with `px-24` (96px*2 = 192px), the content area is 850 - 192 = 658px.
`65ch` is roughly 65 * 16px (or larger with `prose-lg`) ~= 1000px+. So `prose` shouldn't be constraining it narrower than the box.

**Hypothesis:** The user might be referring to the **first line indent** or paragraph spacing?
"不要顶格" can sometimes mean "Add a first-line indent". In Chinese typography, paragraphs usually start with a 2-character indent.
"Don't be top-aligned" could also mean "Add more top padding".

However, looking at the previous turn, I increased padding to `px-24`.
If the user says "Don't be tight", maybe I didn't apply it correctly?
I checked the code, I did apply `px-24 py-20`.

**Alternative Interpretation**:
Maybe the user uploaded a file that *has no newlines*, and `whitespace-pre-wrap` is making it one giant block?
Or maybe they mean the **outer container**?
"Document details text..." usually refers to the content.

Let's try to increase the padding even more to be safe, or ensure the `prose` class isn't doing something weird.
Actually, `prose` adds vertical margins to `<p>`.
I will explicitly set `padding` to be very generous and symmetric.
I will change `px-24` to `px-28` (112px) and `py-24` (96px) to be very spacious.
And I will remove `prose` class if it's interfering, just use standard text styling to have full control. `prose` is good for markdown, but for raw text it might add unwanted margins.

**Actually**, the user said "don't top-align" (不要顶格). In Chinese context, this often means **"Indent the first line of every paragraph"**.
Standard Chinese typesetting requires 2em indentation.
I will add `indent-8` (2rem) or `indent-[2em]` to the paragraph.

**Plan:**
1.  Add `indent-8` (or `indent-[2em]`) to the paragraph to satisfy "不要顶格" (don't start at the very edge/indentation).
2.  Maintain/Ensure generous padding `px-24 py-24`.

**Refined Plan:**
1.  Locate the paragraph in `client/src/pages/DocumentDetail.tsx`.
2.  Add `indent-8` class.
3.  Ensure `px` and `py` are consistent and generous (e.g., `p-24`).

Wait, "两边的页边距保持一致" (Keep side margins consistent).
If I use `text-justify`, it should be consistent.
I will stick to adding indentation and ensuring padding is symmetric.

**Action:** Update `DocumentDetail.tsx` to add text indentation and verify padding.