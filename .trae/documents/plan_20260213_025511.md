I will rebuild the Android app to feature a modern, mobile-first design inspired by top AI applications, while preserving all existing web functionality.

### **1. Backup Existing Mobile Code**
- Rename the current `android` directory to `android.backup` to preserve the previous configuration.

### **2. Implement "Top 5 AI App" Mobile Design**
- **Create Bottom Navigation (`MobileNav.tsx`)**: 
  - Instead of a sidebar, mobile users will see a modern bottom navigation bar with icons for:
    - **Hi Brain** (Home/Chat)
    - **思库** (Documents)
    - **思链** (Knowledge Graph)
    - **思圈** (Community)
    - **设置** (Settings)
  - This mimics the navigation style of apps like ChatGPT and Claude.
- **Update Layout (`Layout.tsx`)**:
  - Detect mobile devices and automatically switch between the Desktop Sidebar and the new Mobile Bottom Navigation.
  - Remove the old "hamburger menu" on mobile to provide a cleaner, app-like experience.

### **3. Responsive Interface Optimization**
- **Document Detail Page**: 
  - Fix the current hardcoded padding (`60px 72px`) which breaks mobile views.
  - Implement responsive padding (e.g., `20px` on mobile) to ensure text is readable and "proportions are normal" on all screen sizes.
- **Dashboard**:
  - Optimize the `AISearch` chat interface height for mobile screens to ensure the input box and chat history are easily accessible.
  - Adjust the grid layout to stack widgets vertically on smaller screens.

### **4. Rebuild Android App**
- Initialize a fresh Android platform using Capacitor (`npx cap add android`).
- Sync the latest web build to the Android project (`npx cap sync`).
- Configure the app ID and name to match the existing project settings.

### **Outcome**
- **Web User**: Continues to see the robust desktop interface with Sidebar.
- **Mobile User**: Experiences a native-feeling app with bottom navigation, optimized touch targets, and proper screen usage.
- **Codebase**: A single shared codebase that intelligently adapts to the device.