Based on the server logs, I have identified two issues causing the search error:

1.  **Invalid API Keys**: The logs explicitly state `InvalidApiKey` for Qwen and `Authentication Fails` for DeepSeek. This means the API keys in your `.env` file are either missing or invalid.
2.  **Missing Model Configuration**: The server code tries to use `qwen-turbo` as a fallback model, but this model is not defined in the `CLOUD_MODELS` configuration, causing an "Unknown cloud model" error.

Here is the plan to fix these issues:

1.  **Update `server.js`**:
    *   Add the missing configuration for `qwen-turbo` to the `CLOUD_MODELS` object.
    *   This ensures that if the primary model fails, the system can correctly attempt to use `qwen-turbo` as a fallback (provided a valid API key is present).

2.  **User Action Required (Environment Setup)**:
    *   **Crucial Step**: You will need to manually edit the `.env` file in the project root.
    *   Replace the placeholder values for `QWEN_API_KEY` or `DEEPSEEK_API_KEY` with your actual, valid API keys.
    *   I cannot do this for you as I don't have your private keys.

I will proceed with patching `server.js` to fix the code error. After that, you must update your `.env` file for the service to work.