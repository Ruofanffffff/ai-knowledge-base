To address the user's four requests for optimizing Hi Brain, I will implement the following changes:

1.  **Speed Optimization (Streaming Response)**:
    *   **Backend (`server.js`)**: Modify `/api/ai/search` and `callCloudModel` to support streaming responses (Server-Sent Events). This will significantly reduce the perceived latency by showing the answer character by character as it's generated.
    *   **Frontend (`AISearch.tsx`)**: Update `handleSend` to consume the streaming API, updating the UI in real-time.

2.  **Smart Retrieval (Intent Detection)**:
    *   **Backend (`server.js`)**: Add a "chat intent classification" step before searching. If the user's query is a greeting, small talk, or a direct question about identity (e.g., "Who are you?"), skip the database/web search and answer directly using the `soul.md` persona.

3.  **No "Thinking Process" Output**:
    *   **Backend (`server.js`)**: Adjust the system prompt in `/api/ai/search` to explicitly instruct the model *not* to output its chain of thought or internal reasoning process.
    *   **Config (`soul.md`)**: Reinforce this instruction in the persona definition.

4.  **Human-like Output Format (No Markdown)**:
    *   **Backend (`server.js`)**: Update the system prompt to request plain text output that mimics human typing (e.g., using natural paragraph breaks instead of bullet points where appropriate, avoiding bold/italic markdown syntax unless necessary for clarity).
    *   **Frontend (`AISearch.tsx`)**: Ensure the message display component renders plain text naturally, though retaining markdown parsing is usually good for code blocks if they ever appear. I will focus on the *generation* side to produce less "bot-like" markdown.

**Implementation Steps:**

1.  **Refactor `server.js`**:
    *   Implement `detectIntent(query)` function.
    *   Refactor `callCloudModel` to support streaming (using `res.write`).
    *   Update prompt to forbid "Thinking:" blocks and Markdown lists.
2.  **Refactor `AISearch.tsx`**:
    *   Switch `fetch` to a streaming reader implementation to handle the real-time response.

This plan addresses all four points: speed (via streaming), relevance (via intent detection), conciseness (via prompt engineering), and human-likeness (via prompt engineering).