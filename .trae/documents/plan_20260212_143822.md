To address the user's issue that "there is no expandable window on the right for AI summary", I need to ensure the functionality to toggle the sidebar is intuitive and visible.

**Current State Analysis:**

* In `DocumentDetail.tsx`, the `showChatPanel` state controls the visibility of the right sidebar (`AnimatePresence` block).

* The button to open this panel is the "Brain" icon in the header toolbar:

  ```tsx
  <button 
    onClick={() => setShowChatPanel(true)}
    disabled={isGeneratingSummary}
    className="text-purple-500 hover:text-purple-600 p-2 relative"
  >
    <Brain size={18} />
  </button>
  ```

* The user might not realize this button opens the summary panel, or they might expect it to be open by default if there are existing summaries.

**Proposed Solution:**

1. **Auto-Open**: If the document *already has summaries*, open the sidebar automatically when the page loads.
2. **Explicit Toggle**: Add a clearer "AI Summary" button or tooltip, or ensure the icon is prominent.
3. **Floating Toggle**: Consider a floating button on the right edge if the header button is too subtle, but the header button is standard.
4. **Fix Logic**: In `useEffect` for loading the document, check if `document.summaries.length > 0` and set `setShowChatPanel(true)`.

**Plan:**

1. **Modify** **`DocumentDetail.tsx`**:

   * In the `loadDocument` function (or a `useEffect` after `document` is set), check if `document.summaries` exists and has items. If so, call `setShowChatPanel(true)`.

   * This ensures that when a user enters a document that already has a summary, they see it immediately in the right sidebar.

**Implementation Details:**

* Find the `loadDocument` function.

* After `setDocument(response.data)`, add logic:

  ```typescript
  if (response.data.summaries && response.data.summaries.length > 0) {
    setShowChatPanel(true);
  }
  ```

* This simple change matches the user's expectation of "seeing the summary" without needing to hunt

