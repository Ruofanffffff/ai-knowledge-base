# 前端显示文档但数据库中不存在的问题诊断

## 问题描述

前端显示了4个文档：
1. 天花板维修流量通道说明
2. 基层机构编制和岗位设置的规范性说明一体化方案（初稿）
3. 20210624年度市级行政单位机构编制动态调整流程方案
4. test.file

但这些文档在数据库中都不存在。数据库中只有146个测试文档（"Test Document"）。

## 诊断结果

### 1. 数据库状态
- ✅ 数据库连接正常
- ✅ 有146个文档记录
- ❌ 前端显示的4个文档都不在数据库中

### 2. 前端缓存检查
- ✅ 前端代码中没有使用localStorage
- ✅ API服务只有1秒的内存缓存（statusCache）
- ✅ 文档列表API直接从后端获取，没有持久化缓存

### 3. 可能的原因

#### 原因1：浏览器缓存（最可能）
前端可能缓存了之前的API响应。浏览器的HTTP缓存可能保存了旧的`/api/documents`响应。

**解决方案：**
1. 在浏览器中按 `Cmd+Shift+R` (Mac) 或 `Ctrl+Shift+R` (Windows) 强制刷新
2. 或者打开开发者工具 → Network → 勾选 "Disable cache"
3. 或者清除浏览器缓存

#### 原因2：后端API缓存
后端可能有缓存机制缓存了文档列表。

**检查方法：**
直接访问API查看返回的数据：
```bash
curl http://localhost:3000/api/documents
```

#### 原因3：前端状态管理
前端可能使用了状态管理（如Zustand），并且状态没有更新。

**解决方案：**
刷新页面或重启前端开发服务器。

## 立即解决步骤

### 步骤1：检查后端API返回
运行以下命令查看后端实际返回的数据：
```bash
cd ai-knowledge-base
curl http://localhost:3000/api/documents | jq '.[0:5]'
```

### 步骤2：清除浏览器缓存
1. 打开浏览器开发者工具（F12）
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

### 步骤3：重启前端服务
如果上述方法无效，重启前端开发服务器：
```bash
# 停止当前服务（Ctrl+C）
# 然后重新启动
npm run dev
```

## 验证修复

修复后，前端应该显示146个测试文档，而不是那4个不存在的文档。

## 预防措施

为了避免将来出现类似问题：

1. **添加缓存控制头**
   在后端API响应中添加：
   ```javascript
   res.setHeader('Cache-Control', 'no-cache, no-store, must-revalidate');
   ```

2. **添加版本号**
   在API URL中添加版本号或时间戳：
   ```javascript
   `/api/documents?v=${Date.now()}`
   ```

3. **使用React Query或SWR**
   这些库提供更好的缓存管理和自动重新验证。

## 下一步

请尝试以下操作：
1. 在浏览器中强制刷新（Cmd+Shift+R）
2. 如果还是显示旧数据，运行 `curl http://localhost:3000/api/documents` 查看后端返回
3. 如果后端返回正确但前端还是错误，重启前端服务器
